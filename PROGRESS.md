# Progress — PROMPT_MAESTRO.md

Tracks phase status against `PROMPT_MAESTRO.md`. Updated at the end of each session.

## FASE 0 — Auditoría e infraestructura

Status: **en curso**

Audit (verified by reading the code, 2026-09-11):

- [x] `ARCHITECTURE.md` exists and documents the stack/directory layout. Does **not** yet cover the patterns/notes/automation data model (none of that exists yet) — will be extended as those land in FASE 4/6.
- [x] Audio clock is already `AudioContext.currentTime`-based (`AudioEngine.ts` `getCurrentTime()`), with `requestAnimationFrame` driving UI polling and `setInterval` used only for metronome lookahead scheduling — this is the standard pattern, not a bug. No rework needed.
- [x] **Undo/redo — implemented.** `src/state/projectStore.ts`: every project-mutating action funnels through a new `setProject()` helper that maintains `past`/`future` snapshot stacks (capped at 200 entries). Continuous edits (fader/knob drag, typing a name, BPM) coalesce into a single undo step within a 400ms window instead of one step per tick; discrete actions (add/remove track or clip, add/remove/reorder effect, mute/solo/arm toggles) always get their own step. `loadProject`/`newProject` reset history. `undo()`/`redo()` re-sync the live audio graph (`syncTracks`/`syncMasterInserts`) after jumping. Wired to Ctrl/Cmd+Z, Ctrl/Cmd+Shift+Z, Ctrl+Y in `DawShell.tsx`, and to Undo/Redo icon buttons in `TransportBar.tsx` (disabled when there's nothing to undo/redo). Covered by 7 new tests in `projectStore.test.ts` (discrete undo/redo, redo-cleared-by-new-action, coalescing, non-coalescing across discrete actions, history-clear-on-new-project) — full suite (256 tests) and typecheck/lint pass.
- [x] **Persistence — migrated to IndexedDB with autosave.** New `src/lib/storage/db.ts` is the single shared IndexedDB connection (`personal-daw`, bumped to v2) with three object stores: `samples` (unchanged), `projects` (new), `sampleAssets` (new). `src/lib/storage/projectStore.ts` and `sampleIndex.ts` now read/write IndexedDB instead of `localStorage`, each with a one-time, idempotent migration of any pre-existing `localStorage` data so upgrading doesn't strand a user's saved projects/samples. All call sites across `BrowserPanel.tsx` (Samples + Projects tabs), `VocalBeatMatchPanel.tsx`, `BeatAnalyzerPanel.tsx`, `BeatGeneratorPanel.tsx`, `PitchStudioPanel.tsx`, `DenoisePanel.tsx` and `state/projectStore.ts` updated for the now-async API. Autosave: the store subscribes to `project` changes and writes to IndexedDB 1.2s after the last edit (debounced), plus a `beforeunload` best-effort flush. New `recoverLastProject()`/`openProjectById()` store actions — the app now **automatically reopens the most recently saved project on load** (previously it always started blank even though "Save Project" worked, which meant "recovery after unexpected close" didn't actually happen). Verified manually in-browser: add track → wait for debounced autosave → confirm the write in IndexedDB directly → reload the page → track is back, undo history correctly reset, Projects tab lists it. Full suite (256 tests) + typecheck + lint clean throughout.

FASE 0 is now functionally complete against its stated scope (ARCHITECTURE.md exists, audio clock was already correct, undo/redo done, persistence migrated). Not yet re-verified against the phase's own acceptance checklist as a final gate — see below.

## FASE 1 — Sistema de diseño y shell móvil

Status: **cumple los criterios de aceptación estipulados**

- [x] Tokens: `globals.css` `:root` now defines named surface/border/text/accent/state tokens (backing values unchanged - same neutral scale - so this didn't require a mechanical rename of every `neutral-900` etc. across 26 files; new/touched code can reference the named tokens).
- [x] Accent color changed app-wide: orange → cyan (`orange-300/400/500` → `cyan-300/400/500`, one `sed` pass across all of `src/components/daw`, then verified with `tsc`/`eslint`/`vitest`) — user explicitly asked this NOT stay orange.
- [x] **TransportBar no longer overflows or scrolls at 360px.** Below `sm`: only Play/Stop/Record + time + a "⋯ More" button are inline (measured `scrollWidth === clientWidth === 360`, no horizontal scroll). Project name, Undo/Redo, BPM, time signature, Loop, Click, Export and Save move into a new `BottomSheet` (`src/components/daw/BottomSheet.tsx` — FASE 1's required "hoja deslizable" base component, tap-backdrop-or-✕ to dismiss, no drag gesture yet). At `sm`+ every control is inline exactly as before (verified at 1024px).
- [x] `paddingTop: env(safe-area-inset-top)` on TransportBar, `paddingBottom: env(safe-area-inset-bottom)` already on the mobile nav and now on BottomSheet.
- [ ] Not done: a full button/slider/knob/context-menu component library. Deliberately deferred — the acceptance criteria is about the *result* (no overflow, thumb-reachable, tokens exist), not a specific implementation, and retrofitting every button in 26 files into shared components now would be a large mechanical refactor with no functional payoff yet. Revisit when a second bottom-sheet/menu use case actually needs the abstraction.
- [ ] 🎤 emoji on "Live Tune" — left as-is, low priority, revisit in a later visual pass.
- [ ] Tabular-numeric enforcement across all numeric readouts — time/BPM already `tabular-nums`; not audited across every dB/Hz readout in the effect panels.

Verified: `tsc --noEmit`, `eslint src`, `vitest run` (256 tests) all clean after every change in this phase. Manually confirmed in-browser at 360px (no scroll, sheet opens/closes) and 1024px (full inline layout unchanged).

## FASE 2 — Timeline de nivel profesional

Status: **subset de mayor valor implementado; ítems de mayor riesgo diferidos (ver abajo)**

- [x] Regla en compases y tiempos: `src/lib/timing/grid.ts` (BPM+time-signature -> beat/bar seconds, 9 tests) reemplaza los ticks de segundos del `Ruler` por números de compás; el largo del compás se muestra explícito.
- [x] Snap a rejilla configurable (Off, 1/4, 1/8, 1/16, 1/32, 1/8t, 1/16t) — selector en la barra inferior del Timeline, aplicado a mover/recortar clips en `ClipView.tsx`.
- [x] Clip gain: ya se aplicaba en el audio engine (`AudioEngine.ts`) pero no existía UI — agregada una línea horizontal arrastrable (-24..+12 dB) sobre cada clip.
- [x] Fade in/out con manija: también ya aplicado en el motor; agregadas manijas triangulares en las esquinas superiores con overlay visual del triángulo de fade.
- [x] Región de loop visible y editable: nuevo `LoopRegion.tsx`, arrastra el cuerpo para mover ambos extremos juntos o cada borde por separado.
- [x] Duplicar clip: nueva acción `duplicateClipAtPlayhead` (store), botón "⧉ Duplicate" + atajo `D`.
- [x] Formas de onda en Canvas con caché de picos — ya existía (`Waveform.tsx` + `computePeaks`), sin cambios.

Deliberadamente diferido (no a medias — no se empezó por el riesgo/costo real):
- [ ] Zoom con pinch e scroll inercial — necesita gestos táctiles reales para probar bien; `PIXELS_PER_SECOND` sigue fijo en 80.
- [ ] Virtualización del renderizado — no urgente con la cantidad de pistas actual (todas las pistas/clips son DOM plano hoy); revisar si el conteo de pistas crece.
- [ ] Crossfade entre clips adyacentes (distinto de fade in/out de un solo clip - requiere detectar solapamiento entre dos clips).
- [ ] Marcadores nombrados arbitrarios (más allá de la región de loop).

Verificado: `tsc`/`eslint`/`vitest` (265 tests, +9 nuevos) limpios. En navegador: generado un beat real (4 pistas con audio real vía Beat Generator), confirmado bar-length en la regla, arrastre de gain simulado con PointerEvent (0dB -> 10dB), undo revierte correctamente a 0dB, loop region con título/rango correcto, sin errores de consola.

## FASE 3 — Mixer completo

Status: **subset de mayor valor implementado; sends/buses diferidos (ver abajo)**

- [x] Fader de recorrido largo, táctil: nuevo `Mixer/Fader.tsx` — todo el control (no solo la manija fina) es el área de arrastre, con delta de arrastre desacoplado del alto visual (mismo patrón que la línea de gain de `ClipView.tsx`) para precisión real en una tira angosta; doble-click/doble-tap resetea a 0dB.
- [x] Pan por canal (ya existía en `TrackHeader.tsx`, faltaba en el Mixer — agregado).
- [x] Mute/Solo/Arm por canal en el Mixer (el Arm ahora usa la acción `armTrack` del store, que ya garantizaba exclusividad — el Mixer no debía reimplementar esa regla con un `updateTrack` suelto).
- [x] Acceso directo a la cadena de inserts: botón "FX" por canal y en Master — selecciona el track (o modo Master) y salta a la pestaña Effects en móvil (`effectsRackMode` y `mobileView` se subieron al store para que el Mixer pueda pilotar la navegación, antes eran estado local de `EffectsRackPanel`/`DawShell`).
- [x] Rename inline por canal en el Mixer (ya existía en el Timeline; de paso se corrigió que renombrar generaba un undo-step por cada tecla — ahora coalesce como BPM/pan).
- [x] Reordenar canales con ◀/▶ (nueva acción `moveTrack`) — reordena el array `tracks` y reasigna `order`; el Timeline refleja el mismo orden porque comparte el campo.
- [x] Master fader real: nuevo `Project.masterVolumeDb` + `GainNode` dedicado en `AudioEngine` (`setMasterVolume`), insertado después de la cadena de inserts de Master y antes del analyser — mismo lugar que un fader maestro en Pro Tools/FL. Proyectos guardados antes de este campo cargan con 0dB por defecto (`loadProject` normaliza).
- [x] Metering peak+RMS con hold y clip: `MeterBar.tsx` reescrito — RMS coloreado (verde/ámbar/rojo) en escala de dB (no lineal), línea de peak-hold con decaimiento (~12dB/s), LED de clip que se enciende a ~-0.18dBFS y queda enclavado hasta que se hace click para resetearlo. Es el mismo componente que ya usaban `TrackHeader` y el medidor de grabación en vivo, así que ambos heredan la mejora sin duplicar código.
- [x] Bug de layout corregido de paso: en móvil, la pestaña "Mixer" quedaba comprimida en la barra compacta de escritorio (192px) mientras el área de arriba —vacía— seguía reclamando su espacio `flex-1`. Ahora el Mixer es una vista a pantalla completa como Browser/Timeline/FX cuando es la pestaña activa en móvil, y solo el dock compacto de escritorio usa la altura fija.

Deliberadamente diferido (arquitectura nueva, no a medias):
- [ ] Sends (2 auxiliares por canal) y buses/grupos — requiere un subsistema de ruteo nuevo (entidad `Bus`, nodos de retorno en `AudioEngine`, UI de asignación) que no existe hoy en ningún lado del código. Implementarlo parcialmente arriesgaría bugs de grafo de audio no verificables sin más tiempo dedicado; se deja para una sesión propia.
- [ ] Salida asignable por canal (enviar a un bus en vez de a Master) — depende directamente del punto anterior.

Verificado: `tsc`/`eslint`/`vitest` (265 tests) limpios. En navegador, con el beat real de 4 pistas: arrastre de fader con clamping -60..+6dB, doble-click reset, undo revierte el fader, pan/mute/solo/arm funcionando (arm respeta exclusividad), reorder ◀/▶ reordena y el Timeline lo refleja, botón FX de canal y de Master saltan correctamente a Effects (seleccionando el track o el modo Master) tanto en escritorio como saltando de pestaña en móvil, fader de Master atenúa el bus completo sin afectar los medidores individuales de cada canal, medidores muestran RMS variando en tiempo real, peak-hold sostenido y LED de clip encendiéndose con señal real (el beat generado sí satura picos) y apagándose al click. A 375px de ancho: el Mixer ahora ocupa toda la altura disponible (antes ~192px con una franja vacía arriba), sin overflow horizontal de página.

## FASE 4 — Creación musical

Status: **primer slice funcional (synth+sampler, patrones, piano roll por tap); pendientes reales anotados abajo**

- [x] Track de instrumento: nuevo `Track.type: "audio" | "instrument"`. Deliberadamente **no** se fusionó `midiClips` dentro de `clips` — quedó como array propio (`Track.midiClips`) para que los 8+ consumidores existentes que asumen `clips: AudioClip[]` (exportar, bounce, mix analysis, los paneles de IA) sigan funcionando sin tocarlos ni tener que filtrar por tipo.
- [x] Instrumento por track: `Instrument = SynthInstrument | SamplerInstrument`, con ADSR compartido. Sintetizador (oscilador + envolvente) y Sampler (sample existente de la librería, re-pitchado por `playbackRate = 2^((nota-raíz)/12)`) implementados en `AudioEngine.playVoice` — se conectan al mismo punto (`graph.input`) que un `AudioClip`, así que atraviesan la cadena de inserts/volumen/pan/mute/medidor del track exactamente igual (todo lo de FASE 3 les aplica gratis).
- [x] Patrones: botón "+ Add Instrument" (crea el track) y "+ Add Pattern" (agrega un `MidiClip` de 1 compás en el playhead del track de instrumento seleccionado y abre el piano roll). El bloque en el Timeline (`MidiClipView.tsx`) se mueve/recorta como un clip de audio y muestra una miniatura de las notas.
- [x] Piano roll: `PianoRoll/PianoRoll.tsx`, abierto como bottom-sheet (reutiliza `BottomSheet`) — grid de tap-para-alternar-nota (como un step sequencer), con preview de audio inmediato al tocar una celda (`AudioEngine.previewNote`), y botones +/− para extender el patrón por compás.
- [x] Panel de instrumento: `EffectsRack/InstrumentSettings.tsx`, agregado arriba de la cadena de inserts cuando el track seleccionado es de instrumento — cambia Synth/Sampler, forma de onda, sample+nota raíz, y los 4 sliders ADSR (reutilizan `ParamSlider`, ya coalescen igual que los demás parámetros de efectos).
- [x] Proyectos guardados antes de este modelo cargan con `type:"audio"`/`midiClips:[]`/`instrument:null` por defecto (mismo patrón que la normalización de `masterVolumeDb` en FASE 3).

Verificado: `tsc`/`eslint`/`vitest` (273 tests, +8 nuevos) limpios. En navegador: creado un track de instrumento, agregado un patrón de 1 compás, programada una melodía tocando celdas (C4-E4-G4-C5), toggle on/off confirmado, cerrado y el bloque en el Timeline muestra la miniatura de notas correctamente tanto en escritorio como a 375px (sin overflow horizontal, la barra de herramientas ahora hace wrap a dos filas). Reproducción real: el medidor del track de instrumento muestra señal real tanto en modo Synth (sawtooth) como después de asignarle un sample existente en modo Sampler y tocarlo pitcheado — confirma que ambos caminos de síntesis llegan al bus del track. Sin errores de consola en ningún paso.

Deliberadamente diferido (para no dejarlo a medias):
- [ ] Arrastrar/redimensionar notas individuales en el piano roll — hoy solo tap para agregar/quitar (duración fija = un paso de grid). Mover una nota existente requiere borrarla y volver a tocarla.
- [ ] Step sequencer dedicado (grid de baterías con velocity por celda) — el piano roll actual puede programar percusión asignando un sample por pitch, pero no tiene la vista compacta tipo FL Studio.
- [ ] Patrones reutilizables de verdad (bloque compartido tipo FL Studio, donde editar una instancia actualiza todas) — hoy cada patrón es independiente; "Duplicate" ya existente para audio no se extendió a `MidiClip`, así que copiar un patrón hoy es solo posible recreándolo o vía código, no desde la UI.
- [ ] Split/Duplicate-at-playhead (los atajos S/D existentes) no se extendieron a `MidiClip` — por ahora solo operan sobre clips de audio.
- [ ] Un limitador de voces/polifonía — cada nota agenda su propio oscilador/buffer sin límite; una progresión muy densa en muchos tracks de instrumento simultáneos podría acumular nodos. No es un problema con el uso típico de este proyecto, pero no hay un techo explícito.

## FASE 5 — Grabación y voz

Status: **comping y Live Tune real ya cumplidos; vocal chain presets ya existían de una sesión anterior**

Auditoría antes de tocar nada: "vocal chain presets" y "Live Tune real" ya estaban sustancialmente hechos de trabajo previo a esta sesión (no se re-hicieron, solo se verificaron y se cerró el único pendiente que quedaba abierto):
- [x] **Vocal chain presets** — ya existía: `VocalEngineerPanel.tsx` analiza la toma y aplica una cadena de efectos real de un tap ("Make Vocal Professional") con presets por carácter (Clean/Natural/Bright...) y por género. Verificado que sigue funcionando, sin cambios.
- [x] **Live Tune real** — ya existía: corrección de tono en vivo genuina vía `realtime-pitch-processor.js` (no un placeholder), con selector de tonalidad/escala, 4 modos (natural/hardTune/modernTrap/extreme) y lectura Sung→Target en vivo. Lo único pendiente de FASE 1 era el emoji 🎤 explícitamente señalado en el prompt maestro ("elimina el emoji 🎤 de Live Tune") — **corregido**: reemplazado por un `MicIcon` propio del set de iconos (`icons.tsx`), consistente con el resto de la barra de transporte.
- [x] **Comping** (lo único genuinamente faltante) — implementado a nivel de toma completa (no a nivel de fragmento, ver diferido abajo): grabar/agregar un clip que se superpone en tiempo con uno existente en el mismo track ya no apila audio simultáneo silenciosamente — `addClip` en el store detecta el solapamiento, agrupa ambos clips con un `takeGroupId` compartido, y silencia (`muted: true`, campo nuevo en `AudioClip`) todas las tomas salvo la más reciente. `AudioEngine.scheduleClip` omite clips silenciados; `TrackLane` no renderiza las tomas inactivas (siguen en los datos, no se pierden). El bloque de la toma activa muestra un selector "Take N/M" para volver a cualquier toma anterior (`selectTake`, nueva acción). Borrar la toma activa promueve automáticamente la toma remanente más reciente en vez de dejar la región muda.

Verificado: `tsc`/`eslint`/`vitest` (278 tests, +5 nuevos de comping) limpios. En navegador: emoji reemplazado por el ícono confirmado visualmente, resto de la app sin regresiones ni errores de consola. La lógica de comping (la parte con riesgo real) se verificó con 5 tests unitarios directos sobre el store — grabar/agregar dos tomas solapadas agrupa y silencia correctamente, clips que no se solapan no se agrupan, `selectTake` cambia cuál sea la activa, borrar la activa promueve otra, y una tercera toma se une al mismo grupo existente — en vez de intentar simular una grabación de micrófono real en este entorno de navegador sandboxeado, que no es fiable para eso.

Deliberadamente diferido (no a medias):
- [ ] Comping a nivel de fragmento (combinar partes de distintas tomas dentro de una misma región, estilo lanes de Pro Tools) — hoy el comping es de toma completa: eliges cuál toma entera suena, no mezclas mitades de dos tomas distintas. Requeriría una vista de carriles dedicada; se deja para una sesión propia si hace falta ese nivel de control.

## FASE 6 — Automatización

Status: **volumen y pan por curva de puntos, funcional de punta a punta**

Auditoría antes de tocar nada: no existía absolutamente nada de automatización en el código (ni tipos, ni motor, ni UI) - fase enteramente nueva.

- [x] Modelo de datos: `Track.automation: { volume: AutomationLane, pan: AutomationLane }`, cada lane con `enabled` + `points: AutomationPoint[]` (`{id, time, value}`). Mientras una lane está deshabilitada o vacía, la reproducción usa el valor estático (`volumeDb`/`pan`) exactamente igual que antes de esta fase - es puramente aditivo, cero riesgo para proyectos existentes (normalizado en `loadProject` igual que `masterVolumeDb`/`instrument`).
- [x] Motor: `AudioEngine.scheduleAutomation` programa la curva completa sobre el `AudioParam` real (`gain`/`pan`) con `setValueAtTime`/`linearRampToValueAtTime`, re-anclada en cada `play()`/`seek()`/`startRecording()` - mismo mecanismo que ya usan los fades de clip. `pause()`/`stop()` ahora re-sincronizan `volumeDb`/`pan` a su valor estático (antes un fader automatizado se quedaba "pegado" en el último valor rampeado tras detener la reproducción).
- [x] Editor: `Automation/AutomationEditor.tsx`, bottom sheet (no un carril inline en el Timeline - ver nota de diseño abajo) con selector Volumen/Pan, toggle de habilitado, y una curva editable: tap en espacio vacío agrega un punto, arrastrar un punto lo mueve, doble-click lo borra. Botón de acceso nuevo en `TrackHeader` (ícono propio `AutomationIcon`, se enciende en cian cuando el track tiene alguna lane habilitada).
- [x] Nota de diseño: se descartó deliberadamente un carril de automatización inline dentro del Timeline (como en Pro Tools) porque el cálculo de alturas de `Timeline.tsx`/`TrackLane.tsx`/`LoopRegion.tsx` asume `TRACK_HEIGHT` uniforme por track (ya verificado y estable desde FASE 2/3) - agregar una fila expandible por track requería alturas dinámicas, con riesgo real de regresión en el ruler/loop/playhead ya probados. El bottom sheet reutiliza el mismo componente y vocabulario de interacción que el piano roll, sin tocar el Timeline en absoluto.

Verificado: `tsc`/`eslint`/`vitest` (286 tests, +8 nuevos: 4 de `interpolateAutomation` puro, 4 del store) limpios. En navegador: agregar/arrastrar/borrar puntos con clicks/pointer events reales, habilitar la lane, y **reproducción real confirmada por el medidor** - el canal de Bass bajó de 84% a 48% de señal entre el punto de automatización en 0s y el de 3.75s, seguido exactamente por el patrón dibujado; al detener, el medidor vuelve a 0% (no se queda "pegado" en el último valor automatizado). Sin errores de consola reales (una sesión de depuración encontró un error de compilación obsoleto en el buffer de logs del navegador que sobrevivió a un reinicio completo del dev server - confirmado como un log histórico atascado, no un error en vivo, verificando el contenido real de la página en cada paso).

Deliberadamente diferido (no a medias):
- [ ] Automatización de parámetros de efectos individuales (ej. cutoff de un filtro) - por ahora solo volumen y pan a nivel de track, los dos targets de mayor valor. Extenderlo requeriría un selector de "qué efecto + qué parámetro" por lane; se deja para cuando haya una necesidad concreta.
- [ ] El fader/pan visual en el Mixer no sigue el valor automatizado en vivo durante la reproducción - solo el audio real sigue la curva, el control estático de la UI no se anima con el playhead (como sí hace Pro Tools). Puro pulido visual, no afecta el resultado sonoro.

## FASE 7 — Capa de IA

Status: **Vocal Match completado esta sesión; AI Mix y masterizado ya cumplían de antes; Beat Gen y AI Assistant contextual con pendientes reales**

Auditoría contra el prompt maestro punto por punto:

- [x] **AI Mix** — el análisis DSP (nivel, masking espectral, gain staging) con sugerencias aplicables ya existía de una sesión anterior. Esta sesión se agregó lo que faltaba: una lectura conversacional real. Nueva sección "AI READ" en `MixAssistantPanel.tsx` que envía el análisis ya medido (severidades, peak/RMS/LUFS, masking, gain staging) como contexto al mismo backend Claude que usa `AiAssistantPanel` (`/api/assistant`, `ANTHROPIC_API_KEY` server-only) — el system prompt (`buildMixSection` en `route.ts`) le pide explícitamente que base su respuesta en esos números reales, no en consejos genéricos. Mismo mecanismo de "propone, no aplica": las acciones sugeridas por Claude se muestran con botón Apply, igual que en el Assistant. Verificado en navegador con el pipeline completo (análisis real → petición al backend → degradación correcta a "not configured" sin la key) — falta solo que el usuario configure `ANTHROPIC_API_KEY` en Vercel para ver una respuesta real de Claude en producción.
- [x] **Masterizado automático con objetivo de loudness** — también ya existía: la sección MASTERING de `MixAssistantPanel` ya sugiere y aplica una ganancia de master hacia un LUFS objetivo por plataforma (Spotify/etc.).
- [x] **Vocal Match — completado esta sesión.** Antes solo comparaba tonalidad (key/scale) y mostraba un mensaje de texto, sin ningún botón de aplicar - violaba la regla "la IA propone, el usuario aplica" tal cual la pide el prompt maestro. Se agregó lo que faltaba explícitamente (nivel y espacio):
  - Nueva `suggestVocalTreatment()` en `vocalBeatMatch.ts`: nivel objetivo de la voz relativo al RMS del beat (+4dB, valor nombrado explícitamente, no una caja negra) y un delay sincronizado al tempo detectado del beat (nota de octavo) como técnica honesta de "espacio" - no se inventó una detección falsa de reverb del beat (eso necesitaría separación de fuentes, inviable aquí).
  - Botones "Apply level" y "Apply tempo-synced delay" en `VocalBeatMatchPanel.tsx`, que crean/encuentran el track de la voz y aplican de verdad: `updateTrack` para el nivel, `setEffectChain` con un Delay para el espacio. Verificado en navegador: RMS/tempo detectados reales, click en ambos botones cambió el volumen del track (-8.1dB aplicado) y agregó un Delay real a su cadena de efectos (267ms, sincronizado al tempo detectado) - visible y editable en la pestaña FX.
- [x] **AI Assistant contextual — completado esta sesión.** El chat vivía solo en su propia pestaña del Browser, sin ningún punto de entrada desde otro lado. Se agregó un botón "Ask AI" (ícono chispa) en `TrackHeader` y en `EffectCard` que saltan directo a la pestaña Assistant con el mensaje pre-rellenado ("Nombre del track: " o "Efecto on Track: "). El borrador del mensaje se subió al store (`assistantDraftMessage`, mismo patrón que `mobileView`/`snapResolution`) en vez de quedar en estado local del panel — evita por completo el problema de sincronización de "¿ya consumí este valor?" entre montado/desmontado del panel, que sí mordió durante la implementación (ver nota abajo). También se subió `BrowserPanel`'s tab activo (`browserTab`) al store por la misma razón: hacía falta poder cambiarlo desde otro componente.
  - Nota técnica: el primer intento (un campo "contexto" de un solo uso, consumido en un `useEffect`/comparación por referencia dentro de `AiAssistantPanel`) falló en dos rondas - el lint del repo prohíbe `setState` dentro de efectos y también prohíbe leer `useRef` durante el render (`react-hooks/set-state-in-effect`, `react-hooks/refs`). La solución correcta terminó siendo más simple que el problema: en vez de sincronizar un valor derivado, subir el dato en sí (`assistantDraftMessage`) al store, eliminando la necesidad de sincronizar nada.
- [~] **Beat Gen** — todavía no es "sampler + patrones" (sigue siendo síntesis, no samples reales) pero se mejoró la síntesis esta sesión, ver detalle abajo. El propio código ya admitía en un comentario que era un placeholder de un solo oscilador/capa por instrumento. Se le preguntó al usuario cómo resolver la fuente de samples reales; sugirió descargar packs desde Reddit. **Se declinó esa vía explícitamente**: los packs de batería que circulan en Reddit casi nunca tienen licencia clara de redistribución (muchos son extraídos de librerías comerciales sin permiso), y no hay forma confiable de verificar la licencia de un archivo enlazado en un post — bundlearlo en un repo público expondría al usuario a un riesgo real de infracción de derechos.
  - Alternativa segura implementada en su lugar: `synthesizeBeat.ts` gana capas de síntesis análoga por instrumento — kick con transiente de click (ruido highpassed además del sweep senoidal), snare con capa de "snap" de alta frecuencia además del cuerpo bandpass+tono, hihat con capa metálica de 3 osciladores cuadrados inarmónicos además del ruido filtrado (técnica clásica de caja de ritmos analógica). Sigue siendo 100% sintetizado — el comentario de honestidad en el archivo se actualizó para no sobre-prometer.
  - Verificado en navegador (tab y dev server completamente limpios): generó 4 tracks nuevos, reproducción real confirmada por medidor (83-90% de señal en drums/bass/master), sin errores de consola.
  - Sigue pendiente si el usuario quiere samples reales: aportar él mismo archivos con licencia verificable (ej. Freesound.org con CC0/CC-BY explícito por archivo, o grabaciones propias) para integrar un sampler real.

## FASE 8 — Exportación, rendimiento y pulido final

Status: **exportación corregida y ampliada; PWA parcial (instalable, sin offline real); pendientes de dispositivo real no aplicables desde aquí**

Auditoría antes de tocar nada — encontró un problema serio, no cosmético: el pipeline de exportación/análisis (`bounce.ts`, usado tanto por "Export to WAV" como por el Mix Assistant) nunca se actualizó junto con el trabajo de FASE 3-6 de esta misma sesión. Un proyecto que usara tracks de instrumento, tomas silenciadas (comping), automatización o el fader de master se exportaría **incorrectamente, en silencio, sin ningún error** — exactamente el tipo de bug que las reglas de honestidad del prompt maestro piden evitar.

- [x] **`bounce.ts` corregido para igualar exactamente lo que se escucha en vivo**:
  - No renderizaba tracks de instrumento en absoluto (`for (const clip of track.clips)` ignoraba `track.midiClips` por completo) — un track de synth/sampler se exportaba como silencio total. Se extrajo la lógica de voz (`playVoice` de `AudioEngine`) a un módulo compartido nuevo (`audio-engine/synthVoice.ts`, `scheduleVoice`) usable tanto por el motor en vivo como por el render offline, para que ambos no puedan divergir.
  - No excluía tomas silenciadas de comping (`clip.muted`) — una toma inactiva sonaría igual que la activa en la exportación.
  - No aplicaba curvas de automatización (volumen/pan) — se extrajo `scheduleParamAutomation` a `lib/automation/automation.ts` (compartido con `AudioEngine`) y ahora se aplica también offline, anclado en t=0.
  - No aplicaba el trim de volumen de master (`masterVolumeDb`) — el master fader se ignoraba por completo al exportar.
  - `projectDurationSec` no contaba la duración de los `midiClips`, así que un proyecto solo de instrumentos se exportaba truncado a los 3s de cola.
  - El botón "Export" además aparecía deshabilitado ("Add audio to the timeline first") para un proyecto compuesto solo por tracks de instrumento, y lo mismo en `MixAssistantPanel`/`mixAnalysis.ts` (que además excluían esos tracks del análisis por-track de masking/gain-staging aunque el mix completo sí los incluyera).
  - Se encontró y corrigió un cuarto problema relacionado: la hidratación de samples antes de exportar/analizar/abrir un proyecto (3 sitios) solo recolectaba `sampleId` de `AudioClip`, nunca el sample asignado a un instrumento Sampler — se extrajo `collectProjectSampleIds()` a `lib/audio/sampleLoader.ts` como único punto de verdad, usado en los 4 lugares.
  - Verificado en navegador capturando el blob real exportado (interceptando `URL.createObjectURL`) y decodificándolo: audio real presente donde debía estar la nota del synth, duración exacta (`4.71428s` = 1 compás a 140bpm + 3s de cola, coincide al detalle de redondeo de muestreo), y magnitud de pico consistente con el trim de master de -12dB aplicado.
- [x] **Export de stems** (nuevo): botón "Export Stems" — renderiza cada track con audio (clips o patrón MIDI) solo por turno a través de `bounceProject` (mismo truco que ya usaba `mixAnalysis.ts` para medir la contribución de un track) y descarga un WAV por track, escalonado 300ms entre descargas. Verificado con 2 tracks de instrumento → 2 archivos WAV reales capturados.
- [x] **PWA parcial**: `public/manifest.json` + ícono propio (`public/icon.svg`, SVG a mano ya que no puedo generar imágenes PNG) + metadata de Next (`manifest`, `appleWebApp`, `viewport.themeColor`) — la app ya es instalable a la pantalla de inicio. **Explícitamente NO incluye** un service worker de verdad, así que "funcionamiento offline" (lo que pide el prompt maestro) sigue sin cumplirse — un service worker mal diseñado que sirva JS desactualizado tras un deploy es peor que no tener ninguno, y diseñar una estrategia de cache correcta para una app Next.js que se actualiza seguido es un trabajo aparte, no algo para apurar.
- [x] **Estado vacío con propuesta de acción**: el mensaje pasivo "No tracks yet" se reemplazó por tres botones reales (+ Add Track, + Add Instrument, Generate a Beat) que hacen algo con un tap, en vez de solo indicar qué hacer en otro lado.

Verificado: `tsc`/`eslint`/`vitest` (289 tests) limpios en cada paso. En navegador: exportación real con audio/duración/master-trim confirmados por decodificación directa del blob, stems reales para 2 tracks, manifest+ícono resuelven con 200, estado vacío funcional en escritorio y móvil (375px, sin overflow).

Deliberadamente diferido (no a medias):
- [ ] **MP3 export** — Web Audio no tiene encoder de MP3 nativo; se necesita agregar una dependencia nueva (ej. `lamejs`) para esto, una decisión que no tomé unilateralmente en esta pasada. Queda como próximo paso si se quiere.
- [ ] **Service worker / offline real** — ver nota arriba; necesita una estrategia de cache diseñada a propósito, no uno genérico.
- [ ] **Pruebas en dispositivo real (Android/iOS), medición de fps y CPU de audio** — no soy capaz de hacer esto desde este entorno (no tengo acceso a hardware físico); necesita que el usuario lo pruebe en sus propios dispositivos.
- [ ] **Gestión de memoria / descarga de buffers no usados** — el cache de buffers decodificados (`AudioEngine.bufferCache`) crece sin límite ni desalojo; no es un problema con el uso típico de un proyecto personal, pero no hay techo. Se deja para cuando haga falta en la práctica.

## FASE 9 — Capa gráfica (bloqueante) + adenda de interacción/voz

Status: **ABIERTA — en curso, con progreso medido real. No se declara cumplida mientras sigan pendientes los puntos listados abajo.**

Esta fase se declaró explícitamente bloqueante para el resto del proyecto ("no se declara terminada ninguna fase anterior mientras esta no se cumpla"). Este apartado se actualiza con **valores medidos** (conteos DOM reales en el navegador a 375-529px de ancho), no afirmaciones, por instrucción explícita del documento de fase.

**9A — Componentes base (hecho):**
- [x] `Knob.tsx` — control rotatorio (arco SVG, arrastre vertical, doble-click resetea, mantener presionado abre edición numérica), hit-area ≥44px aunque el dial visual sea menor (`Math.max(44, size)`).
- [x] `SegmentedControl.tsx`, `Picker.tsx` (reemplazo de `<select>` vía `BottomSheet`), `XYPad.tsx`.
- [x] `CurveEditor.tsx` — editor de curva/nodos en canvas, compartido por EQ (ADSR/compresor pendientes de adoptarlo). Modelo de interacción verificado contra el texto exacto de la fase: doble-tap en vacío crea banda, arrastrar un nodo >40px fuera del canvas lo elimina (además doble-tap sobre un nodo también lo elimina, como vía adicional).
- [x] `MeterBar.tsx` reescrito a canvas (antes divs) con escala de dB dibujada, peak-hold, clip-LED persistente (click para resetear).
- [x] `Fader.tsx` con escala de dB real dibujada (+6/0/-6/-12/-24/-48/-∞).

**EQ (único plugin con visualización completa por ahora):**
- [x] `EqPanel.tsx` — canvas con analizador de espectro en vivo (`getMasterAnalyser`), respuesta combinada **matemáticamente exacta** (un `BiquadFilterNode.getFrequencyResponse()` real por banda, sumado en dB — no una aproximación visual), nodos de banda arrastrables en X (frecuencia, escala log) / Y (ganancia), rejilla de frecuencia (100/1k/10k) y dB (-12/0/+12) dibujada. Por banda: toggle ON/OFF (44×44), tipo vía `SegmentedControl` (HP/LS/PK/HS/LP), Q vía `Knob`, eliminar (44×44).
- [x] **Verificado en navegador, no solo compilado**: canvas real 300×170 presente y visible; lectura de píxeles (`getImageData`) confirma contenido real dibujado (grid + curva cian, no un canvas en blanco); la curva muestra el roll-off real de un highpass en 80Hz (no una línea plana); arrastrar un nodo vía eventos de puntero sintéticos cambia el contenido del canvas (confirma el flujo completo: pointermove → `onPointsChange` → `freq`/`gainDb` → redibujado).

**TrackHeader, Mixer, TransportBar, Timeline — controles nativos y botones <44px (hecho esta pasada):**
- [x] `TrackHeader.tsx` rediseñado — antes 2 `<input type=range>` nativos + 5 botones de 20×20px en 192px de ancho (geométricamente no cabían 5 botones de 44px). Ahora: fila de M/S/Arm/Monitor de 44×44px real (medido: ~45×44px cada uno), volumen y pan movidos a `Knob`s dentro de una hoja "⋯" contextual (no tab-switch, es un bottom sheet anclado) junto con Automation/Ask AI/Delete. `TRACK_HEIGHT` 76→118px para acomodarlo (constante única, cascada limpia a `TrackLane`/`ClipView`/`MidiClipView`/`Ruler`).
- [x] **Monitor de entrada — nuevo, con audio real, no solo visual** (pedido explícito de la adenda): campo `Track.monitorMode: "off"|"auto"|"on"` (default `"auto"`), botón de 3 estados con color/ícono distintos en `TrackHeader` y en `MixerPanel` (grid 2×2 de 44×44, antes 4 botones de 36px de ancho que tampoco cumplían). `AudioEngine`: nuevo `MonitorSession` — un único `getUserMedia` compartido, conectado directamente a `graph.input` de cada track armado elegible (el mismo punto de entrada real que usan clips/instrumentos), así que monitorear pasa de verdad por la cadena de efectos de ese track (autotune/reverb ya aplicados, no una copia seca aparte). `refreshMonitoring()` recalcula la conexión en cada `syncTracks` (arm/monitorMode/play/pause cambian por ahí). Medidor de entrada en vivo (`MeterBar`) junto al botón Arm, alimentado por `getMonitorAnalyser()`/`getRecordingAnalyser()` según corresponda.
- [x] `MixerPanel.tsx` — botones M/S/Arm ya existentes medían 44×36px (altura sí, ancho no); ahora grid 2×2 de 44×44 reales + el nuevo Monitor. Botones "FX" de 32px de alto → 44px. Flechas mover-canal-izq/der (24×24) llevadas a hit-area real de 44×44 vía padding negativo compensado (el visual no cambia, el área táctil sí — mismo truco que ya usa `Knob` con su dial pequeño en una caja de ≥44px), verificado que NO están dentro de ningún ancestro `overflow-hidden` (si lo estuvieran este truco no serviría — se comprobó caso por caso, ver nota de "Open piano roll" abajo).
- [x] `TransportBar.tsx` — Play/Stop/Record eran 40×40 en móvil (contra el objetivo de medición de 375px) y encima *más chicos* en pantallas mayores (`sm:h-9 sm:w-9`); ahora 44×44 uniforme. Undo/Redo y el botón "More" también.
- [x] Barra inferior del Timeline (+ Add Track/+ Add Instrument/+ Add Pattern/✂ Split/⧉ Duplicate) y el CTA de estado vacío: 28px de alto → ≥44px real.
- [x] `BottomSheet.tsx`: botón de cerrar 32×32 → 44×44.
- [x] Color: `TRACK_COLORS[0]` era `#f97316` (naranja, el acento descartado en FASE 1) → `#ec4899`. Proyectos ya guardados conservan el color que tenían asignado (es un dato persistido por track, no derivado en cada render) — no se reescribe retroactivamente.

**Pitch Correction — nuevo efecto de inserción, reemplaza "Live Tune" por completo (adenda punto 2, hecho esta pasada):**
- [x] `public/worklets/realtime-pitch-processor.js` extendido (no reescrito desde cero — conserva el YIN/glide/delay-line ya verificado antes): 5 escalas (major/naturalMinor/harmonicMinor/chromatic/**custom** vía bitmask de 12 bits), `mix` (dry/wet real, antes era 100%-o-nada), `referenceHz` (A4 ajustable, antes 440 fijo), `detectMinHz`/`detectMaxHz` (antes constantes fijas), y el mensaje del puerto ahora incluye `snappedMidi`/`centsOff` para un lector tipo afinador.
- [x] `PitchCorrectionEffect.ts` — envuelve el worklet como un `Effect` normal (mismo patrón que `NoiseGateEffect`: placeholder mientras carga el módulo). `EffectChain`/`AudioEngine`/`bounce.ts` (render offline) actualizados para soportarlo igual que cualquier otro insert — se puede posicionar, reordenar, bypassear, guardar con el proyecto y aplica también a un clip ya grabado, no solo al mic en vivo.
- [x] **"Live Tune" eliminado por completo**, no solo ocultado: `AudioEngine.enableLivePitchMonitor`/`disableLivePitchMonitor`/`updateLivePitchMonitorSettings`/`onLivePitchUpdate`/`isLivePitchMonitorActive` borrados, `LivePitchMonitorPanel.tsx` borrado, tipos `LivePitchMonitorSettings`/`LivePitchInfo` borrados de `types/pitch.ts`. La barra de transporte ya no tiene ningún botón de autotune.
- [x] `PitchCorrectionPanel.tsx` — canvas con historial de cents (línea cian, escala -50¢/0¢/+50¢, línea "en tono" resaltada) + lectura nota-detectada→nota-destino en vivo (leída de `AudioEngine.getEffectNode()`, un accesor nuevo para llegar del estado declarativo al nodo real corriendo); teclado vertical de 12 notas con scroll horizontal (ver nota de ancho abajo), tocar una nota la excluye/incluye y cambia a escala Custom automáticamente; 5 presets con sus nombres exactos de la adenda (Natural/Pop suave/Trap duro/Corrección transparente/Robot); controles de key/scale/retune/mix/humanize/referencia/rango de detección, todos `Knob`/`SegmentedControl`/`Picker`, cero controles nativos.
- [x] **Verificado en navegador con audio real reproduciéndose** (no solo compilado): se agregó el efecto a un track de instrumento, se reprodujo la nota programada, y el canvas mostró la línea de cents moviéndose y asentándose en 0¢ (lectura de píxeles del renglón de texto confirmó contenido real dibujado, no un canvas en blanco); tocar una nota del teclado cambió la escala a Custom y la excluyó/incluyó correctamente; sin errores de consola durante el ciclo completo play→detectar→pausar.
- [ ] **Explícitamente NO implementado: preservación de formantes** — la adenda lo pide, pero necesita separación de envolvente espectral (cepstral/LPC) que el pitch-shifter de delay-line actual no tiene. Nombrado en `AUDIO_ENGINE.md`/`types/effects.ts`, no hay ningún control de UI fingiendo hacerlo.
- [ ] El atajo dentro de la hoja contextual de un clip de voz y dentro del panel de grabación (los otros dos puntos de entrada que la adenda pide para esta función) no existen todavía — dependen de la hoja de clip estilo BandLab (adenda punto 1), que sigue sin construirse. El efecto ya es completamente usable desde "+ Add Effect" mientras tanto.

**Limpieza de botones <44px encontrados durante esta pasada (mismo patrón de hit-area ampliada o rediseño de fila, según si había o no un ancestro `overflow-hidden`):**
- [x] Cabecera de `EffectCard.tsx` (▾ colapsar, ✨ ask-AI, ↑/↓ reordenar, ON/OFF bypass, ✕ eliminar) — medían 11-27px, afecta **los 16 tipos de efecto a la vez** por ser un solo componente compartido. Arreglado vía hit-area ampliada (margen negativo) para los íconos puros y tamaño real 44px para el pill de bypass.
- [x] Pestañas Track/Master de `EffectsRackPanel.tsx` (264×34 → min-h-11).
- [x] Fila de banda de `EqPanel.tsx` rediseñada a 2 renglones (tipo de filtro en su propio renglón de ancho completo) — el `SegmentedControl` de 5 opciones medía 30-32px de ancho al compartir renglón con el toggle ON/Q/eliminar; ahora ~60px cada opción.
- [x] Teclado vertical de `PitchCorrectionPanel.tsx` (36px de ancho al forzar 12 teclas en el ancho del panel) → scroll horizontal con teclas de 44×44 reales.
- [x] Botones LOOP/CLICK de `TransportBar.tsx` (48×24/50×24 → min-h-11), compartidos entre la barra compacta de escritorio y la hoja "Transport" móvil.

**Configuración de entrada, parte 1 (adenda punto 3, hecho esta pasada) — toggles de audio, dentro de la hoja "Transport":**
- [x] Sección "Mic input" nueva en la hoja de `TransportBar.tsx`: 3 botones reales de 44px (Echo cancellation / Noise suppression / Auto-gain), todos apagados por defecto, cada uno llama a `AudioEngine.setMonitorConstraints()` (ya existía el método, faltaba la UI) - verificado en navegador que el toggle cambia de estado y no genera errores de consola.
- [x] `AudioContext` ahora se crea explícitamente con `{ latencyHint: "interactive" }` (antes usaba el default implícito) - pedido textual de la adenda, aunque "interactive" ya era el default del spec, para no depender de que un navegador futuro cambie ese default.
- [x] `AudioEngine.getLatencySec()` — latencia real medida (`ctx.outputLatency`, con fallback a `baseLatency` donde el navegador no expone la primera), mostrada en la misma sección ("40ms measured latency" verificado en navegador con la sesión de audio ya iniciada) - no un número asumido/fijo.

**Deliberadamente NO arreglado esta pasada (para no fingir cumplimiento con un truco):**
- [ ] Ícono "Open piano roll" dentro de `MidiClipView.tsx` (12×12px) — intenté el mismo truco de hit-area ampliada que usé en otros lados, pero este botón vive dentro de un contenedor con `overflow-hidden` (recorta las notas del patrón a los bordes del clip), así que un área táctil agrandada por margen negativo queda recortada y no funciona de verdad — habría pasado la medición de `getBoundingClientRect()` sin dar un área táctil real, que es exactamente el tipo de "cumplimiento falso" que esta fase prohíbe. Arreglo real pendiente: agrandar la fila de cabecera del clip (hoy ~16px) o resolverlo como parte del rediseño de piano roll / interacción de clips (adenda punto 1), no como parche aislado.

**Medido en navegador (375-529px, tabs Timeline/Mixer/FX, con un track de instrumento + EQ + Pitch Correction activos):**
- `document.querySelectorAll('input[type=range]').length` → **0**
- `document.querySelectorAll('select').length` → **0**
- Botones con `width<44 || height<44` visibles: **Timeline → 1** (el ícono "Open piano roll" de arriba, dejado abierto a propósito). **Mixer → 0. FX → 0.**
- `tsc --noEmit` / `eslint src` / `vitest run` (289 tests) limpios después de cada cambio de esta sección.

**Controles nativos restantes en los paneles de IA — eliminados esta pasada:**
- [x] `MixAssistantPanel.tsx` (select de plataforma de masterizado → `Picker`), `VocalBeatMatchPanel.tsx` (2 selects de sample → `Picker`, + botones a 44px), `VocalEngineerPanel.tsx` (select con `<optgroup>` de estilo/género → `Picker` con la nueva prop `group` — encabezados de sección dentro de la hoja, sin perder el agrupamiento visual), `PitchStudioPanel.tsx` (2 selects → `Picker`, 2 ranges → `Knob`), `DenoisePanel.tsx` (1 range → `Knob`), `BeatGeneratorPanel.tsx` (4 selects → mezcla de `Picker`/`SegmentedControl` según cuántas opciones tenía cada uno). `Timeline/ClipView.tsx`'s selector de tomas (comping) también convertido a `Picker` — confirmado que, a diferencia del ícono de `MidiClipView.tsx`, éste SÍ vive fuera de cualquier restricción de altura fija, así que el arreglo es real (la fila de cabecera del clip simplemente crece).
- [x] `Picker.tsx` ganó una prop `group` opcional (encabezado de sección entre opciones cuando cambia) — necesaria para reemplazar el `<optgroup>` de Character/Genre de `VocalEngineerPanel` sin perder esa agrupación.
- [x] Colores naranja restantes eliminados: `Analyzer.tsx`, `PitchStudioPanel.tsx`'s `PitchTrackCanvas` (ambos → cian), `BeatAnalyzerPanel.tsx`'s color de "kick" en su leyenda (→ rosa, mismo tono que el nuevo `TRACK_COLORS[0]`).
- [x] **Medido tras esta limpieza**: `grep` de `type="range"` y `<select` en todo `src/components` → **0 resultados reales** (el único hit es un comentario de documentación en `Picker.tsx` que menciona `<select>` en prosa). `document.querySelectorAll('input[type=range], select').length` en las pestañas Browser/Timeline/Mixer/FX → **0** en todas.

**Pendiente real, todavía abierto (la fase NO se declara cumplida):**
- [ ] Configuración de entrada, resto (adenda punto 3): selección de dispositivo de entrada (hoy siempre usa el default del sistema), compensación automática de la latencia medida en la toma grabada (se reporta la latencia real, pero no se usa todavía para corregir el offset del clip), advertencia de feedback con altavoz (detectar/advertir "estás en altavoz, no audífonos" antes de grabar - el Web Audio API no expone esto de forma confiable, necesita heurística vía `enumerateDevices()`), medidor de entrada junto a Arm mostrado de forma más prominente antes de grabar (hoy existe pero es una barra delgada de 8px), opción de monitoreo "dry" (sin efectos) explícita - hoy el monitoreo siempre pasa por la cadena completa del track.
- [ ] Hoja contextual de clip estilo BandLab (adenda punto 1) — tap-para-abrir, long-press, doble-tap a editor completo.
- [ ] Visualización propia para los otros 14 efectos (Compressor, Multiband, Limiter, Clipper, Noise Gate, De-Esser, Reverb, Delay, Saturation, Exciter, Chorus, Flanger, AutoPan, Stereo Width) y rediseño de Synth/Sampler.
- [ ] Rediseño completo del piano roll (hoy sigue siendo la rejilla de casillas de FASE 4).
- [ ] Waveforms reales de timeline con cache de picos (verificar/actualizar `Waveform.tsx` contra el estándar de esta fase).
- [ ] Preservación de formantes en Pitch Correction (ver nota arriba).
- [ ] EQ/ADSR/compresor no comparten todavía presets (`CurveEditor` está listo para ADSR/compresor pero no adoptado ahí aún); ningún efecto salvo Pitch Correction tiene ≥5 presets, un criterio explícito de la fase para cada plugin.

## FASE 10D — Grabación

Status: **COMPLETA (5/5 subfases), trabajada por subfases (pedido explícito del usuario: proyectos grandes se trabajan en subfases, priorizando ahorro de tokens - no todo en una sola pasada).**

Auditado contra el punto 5 del brief antes de tocar nada: monitorización 3 estados y toggles eco/ruido ya existían de FASE 9 (confirmado, no se re-hicieron). Comping por toma completa ya existía de FASE 5. Lo que audité como genuinamente faltante: medidor de saturación pre-grabación, cuenta atrás audible, compensación de latencia aplicada, selección de dispositivo, aviso de feedback.

- [x] **Compensación de latencia aplicada a la toma** — `state/projectStore.ts`'s `stopRecording`: el `clip.startTime` ahora se calcula como `result.startTime - getLatencySec()` (clamped a 0), en vez de usar `result.startTime` sin corregir. Antes se *medía* la latencia real (`getLatencySec()`, desde FASE 9) pero nunca se *aplicaba* a nada - la toma quedaba desalineada del beat por esa cantidad. Cambio de una línea, verificado con `tsc`/`eslint`/`vitest` (289 tests) limpios - no se hizo verificación en navegador aparte para esta subfase puntual (es aritmética simple sobre un campo numérico ya cubierto indirectamente por los tests de `addClip`/store existentes, y el pedido explícito de ahorrar tokens no justifica un ciclo completo de Playwright para un cambio de este tamaño).
- [x] **Cuenta atrás audible antes de grabar** — `AudioEngine.playCountIn(bpm, beats, onBeat)` (nuevo método público): agenda 4 clics con `playClick` (el mismo que usa el metrónomo, acento en el primero) y resuelve justo un compás después del último clic, que es cuando debe arrancar la captura real. `projectStore.startRecording` ahora hace `await getAudioEngine().playCountIn(...)` antes de llamar a `AudioEngine.startRecording` (que sigue pidiendo el permiso de micrófono y arrancando la reproducción/captura como antes) y expone `isCountingIn`/`countInBeats` en el store. `TransportBar.tsx`: el botón de grabar muestra el número (4,3,2,1) en vez del ícono mientras dura la cuenta, y Reproducir/Detener quedan deshabilitados durante ese lapso (mismo tratamiento que durante `isRecording`). **Verificado en navegador real** (Chromium headless con `--use-fake-device-for-media-stream`, no solo `tsc`/`vitest`): secuencia observada del `aria-label`/texto del botón fue `Cuenta atrás:4 → 3 → 2 → 1 → Detener grabación` con el timing esperado para el bpm por defecto del proyecto, sin errores de consola.
- [x] **Medidor de entrada con detección de saturación explícita antes de grabar** — auditoría corregida a mitad de esta subfase: `MeterBar.tsx` **ya tenía** un LED de clip latcheado (`peakAmp >= 0.98`) conectado al medidor que se muestra junto a Armar desde antes de esta fase; mi nota anterior de "genuinamente faltante" era incorrecta y queda corregida aquí (principio de este proyecto: no afirmar sin verificar). Lo que sí faltaba de verdad, y es lo que arregla esta subfase, es que ese LED es un punto de 6px fácil de no ver — no es "explícito". Cambios: `MeterBar` gana una prop opcional `onClipChange(clipped)` que dispara solo cuando el estado latcheado cambia (no en cada frame); `TrackHeader.tsx` usa ese callback para mostrar una insignia roja de texto "SATURA" junto al medidor de la pista armada, visible antes de pulsar grabar. Verificado en navegador (Chromium headless, fake mic): la fila del medidor renderiza sin errores de consola y sin alterar la altura de 118px de la cabecera de pista. **Límite honesto de esta verificación**: el dispositivo de micrófono falso de Chrome (`--use-fake-device-for-media-stream`) emite un tono sintético de nivel bajo, no satura, así que no pude confirmar visualmente que la insignia roja aparezca con una entrada real saturada - la lógica del LED que la dispara es la misma que ya existía y funcionaba (clip LED), así que el riesgo es bajo, pero no es lo mismo que verlo disparar con clipping real.
- [x] **Selección de dispositivo de entrada** — `AudioEngine` gana `listInputDevices()` (`enumerateDevices()` filtrado a `audioinput`), `getSelectedInputDeviceId()`/`setSelectedInputDeviceId()` (null = default del sistema, re-adquiere el stream de monitor si ya había uno abierto, mismo patrón que `setMonitorConstraints`), y el `deviceId` elegido ahora se pasa en los dos `getUserMedia` reales (`ensureMonitorStream` y `startRecording`) - antes ambos ignoraban cualquier selección y siempre pedían el default. UI: `Picker` nuevo en la hoja "Transporte" → "Entrada de micrófono", listando "Predeterminado del sistema" + cada dispositivo con su label real. **Verificado en navegador** (Chromium headless, 3 micrófonos falsos vía `--use-fake-device-for-media-stream`): el picker mostró exactamente "Predeterminado del sistema, Fake Default Audio Input, Fake Audio Input 1, Fake Audio Input 2" con sus labels reales (no placeholders), sin errores de consola.
- [x] **Aviso de feedback por altavoz (heurística)** — `lib/audio/outputHeuristics.ts`'s `likelyUsingHeadphones()` (función pura, 6 tests unitarios): compara los labels de `audiooutput` de `enumerateDevices()` contra palabras clave de auriculares (`headphone`, `auricular`, `airpods`, `bluetooth`, ...). Devuelve `null` (desconocido, no avisa) cuando el navegador no expone labels usables - honesto con la limitación que el propio brief señala, nunca finge saber que hay altavoz cuando en realidad no hay información. `AudioEngine.listOutputDevices()` nuevo. `TrackHeader.tsx` lo consulta (con re-chequeo en `devicechange`) mientras la pista está armada y monitoreando, y comparte la misma insignia de la subfase 3 con la de saturación - saturación gana si ambas aplican (es el problema más urgente/accionable en ese instante).
  **Verificado en navegador** (Chromium headless, 3 salidas de audio falsas): confirmé en la página real que `enumerateDevices()` devuelve `["Fake Default Audio Output", "Fake Audio Output 1", "Fake Audio Output 2"]` - exactamente el tipo de label sin auriculares que los tests unitarios cubren, y que la función pura mapea a `false` de forma determinista. **No pude ver la insignia "Altavoz" renderizada en pantalla** en esta sesión: el micrófono falso de Chrome resultó saturar de forma continua (pico ≥0.98 permanente, confirmado porque el LED de clip se re-dispara en el siguiente frame incluso justo después de resetearlo a mano), así que la insignia de "Satura" siempre gana la ranura compartida en este entorno de prueba - comportamiento correcto de la prioridad, pero significa que la ruta "Altavoz" específicamente quedó verificada por unit tests + los labels reales del navegador, no por una captura de pantalla con la insignia amarilla puesta.

**FASE 10D cierra aquí** contra el criterio del punto 5 del brief, subfase por subfase: monitorización 3 estados y toggles eco/ruido (ya existían de FASE 9), comping (ya existía de FASE 5), medidor+saturación, cuenta atrás, compensación de latencia, selección de dispositivo y aviso de feedback (las 5 subfases de esta fase). Pendiente real, no cerrado por esta fase: el criterio de cierre completo del brief ("grabar una voz sobre un beat desde un teléfono... y que quede alineada") no se verificó en un teléfono físico real - toda la verificación de esta fase fue en Chromium de escritorio con dispositivos de audio simulados, como el resto del proyecto hasta ahora (10I, la auditoría en dispositivo real, sigue pendiente y depende de que el usuario lo pruebe en el suyo).

## FASE 10F — Efectos: visualización dominante (EN CURSO, por efecto)

Status: **EN CURSO.** Es el ítem más grande de lo que queda de FASE 10 (14 efectos sin visualización propia, según el punto 3 del brief), así que se trabaja un efecto por subfase en vez de intentarlo todo de una vez - mismo criterio de "proyectos grandes en subfases" que FASE 10D.

- [x] **Compresor** (FabFilter Pro-C-style: "curva de transferencia con punto de operación en vivo + medidor de reducción de ganancia"):
  - `audio-engine/effects/curves.ts`'s `compressorTransferDb(inputDb, thresholdDb, ratio, kneeDb)` - la fórmula estándar de knee suave (recta 1:1 por debajo del knee, curva cuadrática dentro del knee, pendiente `1/ratio` por encima), función pura, 6 tests nuevos (continuidad en los bordes del knee, no-boost, ratio=1 es no-op, coincide con la fórmula de knee duro cuando `kneeDb=0`).
  - `CompressorEffect.getReductionDb()` nuevo: expone `DynamicsCompressorNode.reduction` (telemetría real y nativa del navegador, no una aproximación nuestra).
  - `EffectsRack/CompressorPanel.tsx` nuevo: canvas con la curva real (recalculada de los params en vivo, con marcador de umbral y línea 1:1 de referencia) + medidor horizontal de reducción de ganancia leyendo `getReductionDb()` cada frame.
  - **Decisión explícita, documentada en el propio código**: NO se dibuja el "punto de operación en vivo" (el punto que se mueve sobre la curva) que pide la referencia, porque hacerlo con honestidad exige un nivel de entrada medido específicamente en la entrada de este nodo, que hoy no existe (solo hay analisadores por pista, post-cadena) - inventar esa coordenada con el analizador equivocado habría sido precisión falsa, algo que este proyecto evita explícitamente. El medidor de reducción sí es 100% real porque viene directo del nodo nativo.
  - Verificado en navegador (Chromium headless): panel renderizado con la curva doblando visiblemente en el umbral (-24 dB en la captura), línea punteada 1:1, grid con etiquetas de dB, medidor "REDUCCIÓN" en -0.0 dB en reposo (correcto, sin señal comprimiendo todavía) - sin errores de consola. `tsc`/`eslint`/`vitest` (301 tests) limpios.
  - `GainReductionMeter.tsx` extraído a componente compartido (`EffectsRack/`) desde el que vivía dentro de `CompressorPanel.tsx`, porque la subfase siguiente (Limitador) lo reutiliza tal cual - evita duplicar la misma barra dos veces.

- [x] **Limitador** (FabFilter Pro-L-style: "Medidor de sonoridad (LUFS) y de reducción, techo visible"):
  - `LimiterEffect.getReductionDb()` nuevo, mismo patrón que el compresor.
  - `EffectsRack/LimiterPanel.tsx` nuevo: medidor vertical de pico de salida (del analizador de la pista o del master, según dónde esté insertado) con la línea del techo (`ceilingDb`) dibujada en rojo, `GainReductionMeter` compartido, y un renglón "Sonoridad (LUFS aprox.)".
  - **Decisión explícita, documentada en el código**: el renglón de sonoridad **solo se muestra cuando el insert está en el bus Master** (`target === "master"`), no en una pista individual. El único tap de sonoridad que existe en el motor (`AudioEngine.getLoudnessAnalyser()`) mide el master completo, no una pista - mostrarlo también dentro de un Limitador insertado en una pista habría sido una etiqueta "Sonoridad" con el número de una señal completamente distinta (el mix entero, no esa pista), precisión falsa con apariencia de real. El medidor de pico y el de reducción sí son reales en cualquiera de los dos casos porque usan el analizador/telemetría correctos para ese target.
  - **Verificado en navegador** en ambos targets: insertado en una pista, "Sonoridad" no aparece (confirmado ausente); insertado en Master, "Sonoridad (LUFS aprox.)" aparece con su valor; canvas con línea de techo roja y grid de dB visibles en la captura; medidor de reducción mostrando -1.2 dB reales (señal de monitor activa en la sesión de prueba); sin errores de consola en ningún caso. `tsc`/`eslint`/`vitest` (301 tests) limpios.

- [x] **Puerta de ruido / Noise Gate** (FabFilter Pro-G-style: "umbral dibujado sobre la señal entrante, indicador de apertura"):
  - `NoiseGateEffect` (worklet-backed) ganó un `AnalyserNode` propio **en serie** delante del `AudioWorkletNode` (`input -> analyser -> worklet -> output`, un `AnalyserNode` pasa el audio sin modificarlo). Es deliberado y distinto del patrón de Compresor/Limitador: el analizador por pista (`getTrackAnalyser`) mide *después* de toda la cadena de inserts, así que en cuanto la puerta cierra mostraría silencio casi total sin importar qué señal esté llegando realmente - exactamente lo contrario de lo que este panel necesita mostrar. Con el analizador en serie propio de esta instancia, la señal medida es siempre la que de verdad le llega a la puerta, se abra o no.
  - `public/worklets/noise-gate-processor.js`: el worklet ya calculaba `this.envelope` (0..1, la ganancia que aplica en cada muestra) pero nunca salía del `AudioWorkletGlobalScope`. Se le añadió `port.postMessage(this.envelope)` cada ~50 ms (limitado con un contador de muestras, no en cada bloque de proceso - un bloque son ~2.9 ms a 44.1kHz, o sea ~375 mensajes/s sin el límite, saturando el message port para nada).
  - `NoiseGateEffect.getEnvelope()` nuevo: lee el último valor recibido por `port.onmessage` - telemetría real del propio worklet, no una comparación umbral reconstruida en el hilo principal (que podría no coincidir con el comportamiento real de ataque/liberación/retención corriendo en el audio thread).
  - `EffectsRack/NoiseGatePanel.tsx` nuevo: canvas con grid en dB (-60/-40/-20/0), historial deslizante (150 muestras, tomadas del analizador en serie vía `computePeakDb`, no un timbre lineal) y la línea de umbral punteada, coloreada verde/roja según si la puerta está abierta. Insignia "ABIERTA/CERRADA" + porcentaje, alimentada por `getEnvelope()` (abierta cuando `envelope >= 0.5`).
  - **Decisión de diseño corregida durante la propia subfase**: la primera versión dibujaba la forma de onda cruda en escala lineal (-1..1) con el umbral como líneas ±amplitud. Se descartó tras verla en el navegador: con un umbral típico de -45 dB (≈0.0056 en lineal), la señal entrante queda a un par de píxeles del centro, indistinguible visualmente del silencio. Se rehizo con la misma convención de eje en dB que ya usan CompressorPanel/LimiterPanel (historial de nivel pico en dB en vez de onda cruda) - mismo dato real, pero legible.
  - Verificado en navegador (Chromium headless, mic falso): con monitorización activa se ve el historial de nivel con picos reales cruzando por encima de la línea de umbral, la insignia alternando "ABIERTA"/"CERRADA" con el porcentaje moviéndose en vivo, sin errores de consola. `tsc`/`eslint`/`vitest` (301 tests) limpios.

- [x] **De-esser** (FabFilter Pro-DS-style: "banda de detección visible sobre el espectro"):
  - `DeEsserEffect` ganó el mismo patrón en serie que Noise Gate: un `AnalyserNode` propio (`inputAnalyser`, `fftSize=2048`) insertado entre `input` y el split lowBand/highBand (`input -> analyser -> {lowBand, highBand}`). Sin esto, la única opción habría sido el analizador de la pista/master (post-cadena) - que ya mostraría el resultado *después* de que el de-esser duckea los agudos, escondiendo justo la sibilancia que la banda de detección necesita mostrar.
  - `DeEsserEffect.getReductionDb()` nuevo: expone `sibilanceCompressor.reduction`, mismo patrón nativo que Compressor/Limiter (el de-esser ya usaba un `DynamicsCompressorNode` de verdad para la banda alta).
  - `EffectsRack/DeEsserPanel.tsx` nuevo: espectro en barras (escala log de frecuencia, mismo cálculo bin-a-bin que `EqPanel.tsx`) leído del `inputAnalyser` real, con la región por encima del cruce (`params.freq`) sombreada como "banda de detección" - las barras dentro de esa banda se pintan en rojo cuando hay reducción activa (`getReductionDb() < -0.1`), en blanco cuando no. Línea de cruce con etiqueta de frecuencia. `GainReductionMeter` compartido debajo.
  - Verificado en navegador (Chromium headless, mic falso + monitorización activa): espectro real con barras variables, banda roja por encima de 6.5kHz, medidor de reducción mostrando -8.9 dB reales, sin errores de consola. `tsc`/`eslint`/`vitest` (301 tests) limpios.

- [x] **Saturación** (Soundtoys Decapitator-style: "curva de distorsión + comparación de espectro antes/después"):
  - `SaturationEffect` ganó dos analizadores en serie: `inputAnalyser` antes del split dry/wet, `outputAnalyser` después de la mezcla final - mismo patrón que Noise Gate/De-esser, necesario para que "antes" y "después" sean de verdad la entrada y la salida de esta instancia y no lo que sea que el bus de pista/master esté haciendo con otros efectos alrededor.
  - Canvas 1: curva de transferencia dibujada directamente desde `makeSaturationCurve(tone)` - las mismas muestras que usa el `WaveShaperNode` en vivo, no una reconstrucción aparte - con línea 1:1 de referencia.
  - Canvas 2: espectro antes (blanco) vs después (naranja) superpuestos, mismo binning log-frecuencia que EqPanel/DeEsserPanel pero como líneas en vez de barras (más legible para comparar dos series que barras opacas).
  - **Corrección de precisión al revisar el brief real**: la sesión venía asumiendo "14 efectos" pendientes según una nota de resumen anterior, pero la tabla real del punto 3 de `fase-10-reconstruccion-interfaz.md` solo lista 13 efectos con referencia explícita, y Multiband/Clipper/Exciter/Chorus/Flanger/AutoPan **no están en esa tabla** - no tienen un producto de referencia asignado por el brief. Reordenada la prioridad para seguir con los que sí están explícitamente pedidos (Reverb, Delay, Saturación, Imagen estéreo) antes que esos seis, que de todas formas van a necesitar alguna visualización real más adelante aunque el brief no dicte un modelo específico.
  - Verificado en navegador (Chromium headless, mic falso): curva en S visible con drive alto, espectro con dos trazos claramente distintos (antes más uniforme, después con más energía en agudos - armónicos reales de la saturación), sin errores de consola. Ajuste menor de posición de etiquetas tras verlas cortadas en la primera captura. `tsc`/`eslint`/`vitest` (301 tests) limpios.

- [x] **Imagen estéreo / Stereo Width** (iZotope Ozone Imager-style: "vectorscopio y correlación estéreo"):
  - `src/audio-engine/stereoAnalysis.ts` (nuevo, puro): `computeStereoCorrelation(left, right)` - correlación de fase estándar (producto punto normalizado, +1 idéntico/mono-compatible, -1 invertido, 0 sin relación), 6 tests (idénticos, invertidos, ortogonales, silencio total, clamp, entrada vacía).
  - `StereoWidthEffect`: donde antes `mid`/`widthPos`/`widthNeg` se sumaban implícitamente en las entradas del `ChannelMergerNode`, ahora se suman primero en dos `GainNode`s explícitos (`leftSum`, `rightSum`) para tener un nodo único por canal que tapear. `leftAnalyser`/`rightAnalyser` insertados en serie justo ahí (antes del merger) - el mismo patrón en serie de esta racha de subfases, pero esta vez imprescindible: un analizador de pista (mono, post-suma) no puede dar L/R separados en absoluto, no es solo una cuestión de honestidad de datos sino una imposibilidad técnica.
  - `EffectsRack/StereoWidthPanel.tsx` nuevo: vectorscopio (nube de puntos mid/side rotada 45°, eje M vertical = mono, eje S horizontal = fuera de fase) leyendo ambos analizadores cada frame, más un medidor de correlación horizontal (-1 a +1) alimentado por `computeStereoCorrelation`.
  - **Verificado en navegador** (Chromium headless, mic falso + monitorización activa): sin errores de consola. La captura visual muestra correlación "1.00" y una línea vertical tenue en vez de una nube - **correcto para la señal de prueba**, no un fallo: el mic falso de Chromium es mono, así que `L === R` en todo el buffer, lo que colapsa el eje S a 0 (línea vertical exacta) y da correlación +1 exacta - exactamente el comportamiento correcto que el propio comentario de `StereoWidthEffect` ya documentaba antes de esta subfase. Confirmado con una lectura directa de píxeles del canvas (`getImageData`) que sí hay puntos reales dibujados a lo largo de esa línea, no un canvas vacío. No se pudo verificar visualmente el caso de "nube ancha" con contenido genuinamente estéreo porque el entorno de prueba no tiene una fuente estéreo real disponible - limitación del entorno de verificación, no del componente. `tsc`/`eslint`/`vitest` (307 tests) limpios.

- [x] **Reverberación** (Valhalla Room/Pro-R-style: "cola de decaimiento dibujada, tamaño de espacio como gráfico"):
  - `impulseResponse.ts` ganó `reverbDecayEnvelopeDb(t, sizeType)` (puro, 5 tests nuevos): la fórmula exacta `(1-t)^exponente` que `generateImpulseResponseSamples` ya usaba para modular su ruido - se exportó `DECAY_EXPONENT` para que ambas funciones compartan la misma tabla en vez de duplicar los números. No es una curva redibujada aparte: es la fórmula real que termina en el buffer que carga el `ConvolverNode`.
  - `ReverbEffect` ganó `wetAnalyser` en serie justo después de `wetGain` (antes de sumarse con la señal seca) - nivel real de la cola en este instante, no el nivel mezclado seco+húmedo que dar��a un analizador de pista.
  - `EffectsRack/ReverbPanel.tsx` nuevo: las tres curvas de decaimiento (sala/auditorio/placa) dibujadas a la vez con la seleccionada en blanco brillante y las otras dos tenues - convierte el selector de tamaño en una comparación visual real en vez de solo tres botones con texto, que es lo que pide "tamaño de espacio como gráfico". Barra vertical pequeña con el nivel de la cola en vivo, telemetría real de `wetAnalyser`.
  - Verificado en navegador (Chromium headless, mic falso + monitorización activa): al cambiar a "auditorio" la curva blanca es visiblemente la de caída más lenta (permanece más alta que las otras dos, correcto - hall tiene el exponente más bajo), la etiqueta de segundos coincide con el slider de Caída, la barra verde de cola en vivo se mueve con la señal real, sin errores de consola. `tsc`/`eslint`/`vitest` (312 tests) limpios.

- [x] **Delay** (Soundtoys EchoBoy-style: "ecos sobre rejilla de tempo, divisiones musicales, realimentación visual"):
  - `src/audio-engine/effects/tempoGrid.ts` (nuevo, puro): tabla `TEMPO_DIVISIONS` (1/1 hasta 1/16T, incluyendo puntillos y tresillos, convención estándar de cualquier plugin de delay), `divisionToMs(beats, bpm)`, `msToClosestDivision(ms, bpm)`. 6 tests.
  - `DelayEffect` ganó `wetAnalyser` en serie tras `wetGain`, mismo patrón que Reverb - nivel real de los ecos antes de mezclarse con la señal seca.
  - `EffectsRack/DelayPanel.tsx` nuevo: ecos dibujados como barras en posiciones exactas `timeMs * N` con amplitud `feedback^(N-1)` (la envolvente ideal que produce una línea de delay con feedback - documentado en el propio código como diagrama de picos, no una captura de la señal real, mismo tipo de honestidad que la curva de Reverb), superpuestos sobre una rejilla de tempo calculada con el **BPM real del proyecto** (`useProjectStore`, no un tempo fijo o inventado). Tocar la rejilla ajusta `timeMs` a la división musical más cercana. Barra pequeña de nivel en vivo del camino húmedo, telemetría real.
  - Verificado en navegador (Chromium headless, mic falso + monitorización activa): ecos visibles decayendo, rejilla con etiquetas de división a 140 BPM (el tempo real del proyecto de prueba), al tocar la rejilla el tiempo saltó a 350ms con la etiqueta "Más cercana ahora: 1/8." - verificado matemáticamente correcto (0.75 beat a 140 BPM = 321.4ms, la división más cercana a 350ms de la tabla completa), sin errores de consola. `tsc`/`eslint`/`vitest` (318 tests) limpios.

**Los 13 efectos con referencia explícita del punto 3 del brief están completos.**

Desde aquí, los siguientes efectos (Clipper, Multiband, Exciter, Chorus, Flanger, AutoPan) **no tienen un producto de referencia asignado por el brief** - se les da una visualización real igualmente (el principio de honestidad del proyecto no depende de que el brief lo pida efecto por efecto), reutilizando los patrones ya establecidos en vez de inventar uno nuevo por efecto.

- [x] **Clipper**:
  - `ClipperEffect` ganó `outputAnalyser` en serie tras `makeupGain` - la salida real de esta instancia, post-techo.
  - `EffectsRack/ClipperPanel.tsx` nuevo: mismo patrón de historial de nivel en dB + línea de techo que ya se usó en Limiter/Reverb/Delay (no hay referencia de producto específica para un clipper standalone, así que se reutiliza el patrón en vez de inventar uno). Insignia "RECORTANDO/LIBRE": con un clipper duro, el nivel de salida pegado exactamente al techo **es** recorte activo (no puede pasar de ahí), así que la insignia lee eso directo del historial real, no es una suposición.
  - Verificado en navegador (Chromium headless, mic falso + monitorización activa): picos reales aplanándose exactamente en la línea de techo (-0.5 dB en la captura), sin errores de consola. `tsc`/`eslint`/`vitest` (318 tests) limpios.

- [x] **Chorus, Flanger, Paneo automático** (los tres efectos LFO de la fase, resueltos juntos por compartir la misma base técnica):
  - Los tres (`ChorusEffect`, `FlangerEffect`, `AutoPanEffect`) ganaron `getLfoPhase()`: la fase real (0..1) del LFO de seno que ya corría desde `construct()`. Web Audio no expone la fase de un `OscillatorNode` en ningún momento - no hay `getPhase()` ni nada parecido - pero como el LFO arranca en un instante conocido (`lfoStartTime = ctx.currentTime`) y corre a una frecuencia conocida sin interrupciones, `(ahora - inicio) * rateHz mod 1` **es** esa fase exactamente (con una pequeña suavización de ~10ms justo después de cambiar la velocidad, por el `setTargetAtTime` del propio parámetro). No es telemetría de audio ni una aproximación: es la fase real calculada con los mismos números que controlan el oscilador.
  - `ChorusPanel.tsx`/`FlangerPanel.tsx`: la curva de tiempo de delay modulado (`baseDelayMs + depthMs·sin(2πt)`) sobre un ciclo completo del LFO, con un punto verde en la fase real actual - no un punto adivinado, exactamente donde está el LFO ahora mismo. `ChorusEffect`/`FlangerEffect` exportan su `BASE_DELAY_MS` para que el panel no reinvente el número.
  - `AutoPanPanel.tsx`: misma idea con la curva de posición de paneo (-1 izquierda a +1 derecha) en vez de milisegundos.
  - Ningún efecto LFO tiene referencia de producto en el brief, pero se les da visualización real igual, reutilizando el mismo patrón entre los tres en vez de inventar tres distintos.
  - Verificado en navegador (Chromium headless): las tres curvas se ven correctamente (seno completo, punto verde sobre la curva, no fuera de ella), el punto se mueve con el tiempo real entre capturas sucesivas, sin errores de consola. `tsc`/`eslint`/`vitest` (318 tests) limpios.

- [x] **Excitador / Exciter**: combina los dos patrones ya establecidos esta fase - banda sombreada sobre el cruce (como De-esser) + espectro antes/después superpuesto (como Saturación) - en vez de inventar uno nuevo. `ExciterEffect` ganó `inputAnalyser`/`outputAnalyser` en serie, mismo patrón que `SaturationEffect`. Verificado en navegador: banda sombreada por encima de 4.5kHz, líneas de espectro real antes/después visibles (la diferencia es sutil con mezcla al 25%, que es honesto - un exciter aditivo con poca mezcla no debería verse como una transformación drástica), sin errores de consola. `tsc`/`eslint`/`vitest` (318 tests) limpios.

- [x] **Compresor multibanda / Multiband** (el último, dejado para el final a propósito por ser el más grande):
  - `MultibandCompressorEffect.getReductionDb()` nuevo: devuelve `{ low, mid, high }`, telemetría real de los tres `DynamicsCompressorNode` nativos, uno por banda.
  - `EffectsRack/MultibandPanel.tsx` nuevo: reutiliza `compressorTransferDb` (la misma función pura de `CompressorPanel`) para dibujar las tres curvas de transferencia a la vez en un solo canvas, coloreadas por banda, con leyenda - mismo criterio de "superponer en vez de pestañas" que ya usó `ReverbPanel` para sus tres tamaños de espacio. Tres `GainReductionMeter` compartidos debajo, uno por banda con su propio editor.
  - **Detalle de precisión corregido al construir el panel**: `MultibandBandParams` no tiene campo `kneeDb` (a diferencia de `CompressorParams`), y `MultibandCompressorEffect` nunca configura `.knee` en sus nodos - así que corren con el valor por defecto de la especificación Web Audio, 30 dB. Se usó ese mismo número (`DEFAULT_KNEE_DB = 30`, documentado en el código) para que la curva dibujada coincida con lo que los nodos reales hacen, en vez de inventar un valor de knee arbitrario.
  - Verificado en navegador (Chromium headless, mic falso + monitorización activa): las tres curvas y su leyenda visibles, medidor de reducción de "Graves" mostrando -2.0 dB reales con el mic falso activo, sin errores de consola. Tras esta subfase, `EffectParamsEditor.tsx` quedó sin ningún `ParamSlider` suelto - cada tipo de efecto tiene su propio panel dedicado, import muerto eliminado. `tsc`/`eslint`/`vitest` (318 tests) limpios.

**FASE 10F completa: 14/14 efectos con visualización real**, contando los 13 con referencia explícita del punto 3 del brief más los 6 que no la tenían (Clipper, Chorus, Flanger, AutoPan, Excitador, Multibanda) a los que igual se les dio una honesta en vez de dejarlos con `ParamSlider`s genéricos.

## FASE 10F (continuación) — Synth/Sampler (EN CURSO, por sub-pieza)

Status: **EN CURSO.** Es la pieza más grande de lo que queda de FASE 10 (brief punto 3: "forma de onda dibujada, envolventes editables con nodos, filtro con curva" para Synth; "forma de onda con marcadores de inicio/fin/bucle, pads, troceado por gesto" para Sampler), así que se trabaja en sub-piezas independientes en vez de todo de una vez - mismo criterio que el resto de FASE 10.

- [x] **Synth 1/2 — forma de onda dibujada + envolvente ADSR con nodos**:
  - `src/audio-engine/waveformShapes.ts` (nuevo, puro): `oscillatorSample(type, phase)` - la forma canónica/idealizada de cada oscilador (sine/square/sawtooth/triangle), documentado explícitamente como tal: el `OscillatorNode` real de Web Audio band-limita internamente (estilo PolyBLEP) para evitar aliasing, lo que mete un rizado sutil en el audio real que esto no intenta replicar muestra a muestra - dibujar la forma idealizada es la práctica estándar de cualquier UI de synth, no una tergiversación. `adsrLevel(t, attack, decay, sustain, release)` y `adsrTotalSec(...)`: la envolvente lineal a trozos exacta, la misma matemática que `synthVoice.ts`'s `scheduleVoice` ya programa de verdad sobre el `GainNode` real vía `linearRampToValueAtTime` (confirmado leyendo ese archivo antes de escribir esta función, no asumido) - 11 tests nuevos.
  - `EffectsRack/InstrumentSettings.tsx`: nuevo canvas de forma de onda (dos ciclos, redibuja al cambiar el tipo de oscilador).
  - `EffectsRack/EnvelopeEditor.tsx` (nuevo, compartido entre Synth y Sampler ya que ambos usan el mismo ADSR): reemplaza los 4 `ParamSlider`s numéricos por un `CurveEditor` con 3 nodos arrastrables (fin de ataque, fin de caída/nivel de sostenido, fin de liberación) sobre la curva exacta de `adsrLevel`. Ventana de tiempo mostrada auto-escalada al total actual (con margen, ver siguiente punto).
  - **Bug real encontrado y corregido durante la verificación en navegador, no al escribir el código**: la primera versión recalculaba los tres parámetros (`attack`/`decay`/`release`) desde la posición de los TRES nodos en cada arrastre, no solo el que se movía. Al arrastrar únicamente el nodo de ataque más allá de la posición fija del nodo de caída, `decayP.x * totalSec - newAttack` se volvía negativo y `decay` se reseteaba a 0 sin que el usuario lo tocara - confirmado reproduciéndolo con Playwright (arrastrar ataque de 0.01s a 0.45s dejaba "Caída 0.00s" y "Rel. 0.000s", que antes del arrastre eran 0.15s y 0.20s). Corregido: `handlePointsChange` ahora solo recalcula el/los parámetro(s) del nodo que efectivamente cambió, pasando los otros tal cual vienen de los props (su valor real actual), no reconstruidos desde una posición de pantalla que puede quedar obsoleta. Re-verificado: arrastrar ataque deja caída/liberación exactamente iguales; arrastrar el nodo de caída cambia caída+sostenido y deja ataque/liberación intactos.
  - También se corrigió, antes de que llegara a verse en pantalla: el nodo de liberación se construía en `x=1` (el borde derecho exacto), y `CurveEditor` limita el arrastre a `[0,1]` - así que solo se podía arrastrar hacia adentro (liberación más corta), nunca hacia afuera. Se añadió un margen (`WINDOW_HEADROOM = 1.3`) para que los tres nodos tengan espacio de sobra a ambos lados.
  - Verificado en navegador (Chromium headless): forma de onda cambia correctamente entre sierra/cuadrada al tocar el selector; arrastre del nodo de ataque confirmado moviendo el valor real (lectura del store, no solo visual) sin afectar caída/liberación; arrastre del nodo de caída confirmado cambiando caída+sostenido sin afectar ataque/liberación; sin errores de consola. `tsc`/`eslint`/`vitest` (329 tests, 11 nuevos) limpios.

- [x] **Synth 2/2 — filtro con curva**:
  - `SynthInstrument` ganó `filterCutoff`/`filterResonance` (default 20000Hz/Q=1, "completamente abierto" - no cambia el sonido de los synths existentes). Migración añadida en `projectStore.loadProject` para proyectos guardados antes de este campo.
  - `synthVoice.ts`: un `BiquadFilterNode` lowpass real entre el oscilador y la envolvente, fijo durante toda la vida de la voz (misma convención que la forma de onda - no se automatiza en vivo a media nota).
  - `EffectsRack/FilterCurveEditor.tsx` (nuevo): curva de respuesta real vía `BiquadFilterNode.getFrequencyResponse()` (nodo desechable, nunca conectado al grafo - misma técnica exacta que `EqPanel`), con un nodo arrastrable cuya posición Y queda **anclada a la propia curva** (la altura real de respuesta en el corte, no un valor de resonancia inventado en el mismo eje) - evita el problema de que el punto flote desconectado de la curva que dibuja. La resonancia tiene su propio `ParamSlider` en vez de compartir el eje Y del nodo, porque mapear Q a "altura sobre la curva" habría necesitado una fórmula aproximada de altura de pico en vez de la matemática exacta ya disponible.
  - Verificado en navegador: curva plana en 0dB con el filtro por defecto (corte en 20kHz, fuera de rango audible real), consistente con "completamente abierto". `tsc`/`eslint`/`vitest` (329 tests, incluyendo el test existente de `createTrack` actualizado con los nuevos campos) limpios.

**Sintetizador (Synth) queda con las tres piezas del brief completas**: forma de onda dibujada, envolvente con nodos, filtro con curva.

## PIVOTE DE PRIORIDADES (indicado por el usuario, no una decisión propia)

A partir de este punto el desarrollo de generación musical automática (Beat Generator, generación de pistas/instrumentales completas por IA) queda **en pausa** - no se borra nada funcional, simplemente deja de ser foco. El centro de gravedad pasa a la voz: grabación, edición y procesamiento vocal como prioridad 1-4, seguido de mezcla/routing, plugins/instrumentos, IA aplicada a voz, y MIDI/producción - en ese orden. Sampler 1/2 y 2/2 (marcadores de inicio/fin/bucle, pads, troceado por gesto) quedan pendientes pero no se continúan ahora mismo por este pivote, no porque el trabajo esté descartado.

## Grabación (primeros huecos cerrados tras el pivote)

- [x] **Ganancia de entrada (input gain/trim)**:
  - `AudioEngine`: `inputGainDb` (estado del engine, no del proyecto - vive igual que `monitorConstraints`/`selectedInputDeviceId`, no se persiste con el proyecto). Un `GainNode` real (`inputGain`) insertado entre el `MediaStreamAudioSourceNode` y todo lo que va después - el analizador de monitoreo y la conexión a pistas armadas en `MonitorSession`; el worklet de grabación, su analizador y el `silentSink` en `RecordingSession` - así que una sola ganancia afecta a la vez lo que se oye en vivo y lo que efectivamente se graba, no dos caminos separados que podrían desincronizarse.
  - `setInputGainDb(db)` aplica el valor en vivo (`gain.value = dbToGain(db)`) a cualquier sesión de monitor y/o grabación que esté abierta en ese momento, sin necesidad de reabrir el micrófono.
  - `TransportBar.tsx`: slider (-24 a +24 dB) junto al selector de dispositivo de entrada existente.
  - Verificado en navegador (Chromium headless, mic falso): arrastrar el slider con teclado (12 pasos de 0.5dB) deja la lectura en "+6.0 dB" exacto, sin errores de consola. `tsc` limpio.

- [x] **Selección de dispositivo de salida**:
  - `AudioEngine.isOutputDeviceSelectionSupported()`: comprueba si `AudioContext.prototype.setSinkId` existe de verdad antes de ofrecer el control - `setSinkId` en `AudioContext` está estandarizado pero no universal (Chrome/Edge lo implementan, Safari/Firefox no al momento de escribir esto), y el principio del proyecto es no fingir una capacidad que el navegador no tiene.
  - `setSelectedOutputDeviceId(deviceId)`: llama al `setSinkId` real del `AudioContext` (contexto único del engine, así que mueve TODO lo que suena - master bus completo), aplicado también de forma diferida si se elige un dispositivo antes de que exista el contexto (primer gesto de usuario).
  - `TransportBar.tsx`: nueva sección "Salida de audio" - picker real si el navegador soporta `setSinkId`, si no, un aviso honesto explicando por qué no está disponible en vez de un control que no haría nada.
  - Verificado en navegador (Chromium headless con dispositivos de salida fake de Chromium): `setSinkId` detectado como soportado, picker abre y lista los 3 dispositivos de salida fake reales del navegador ("Fake Default Audio Output", "Fake Audio Output 1/2"). `tsc` limpio.

- [x] **Comping real por fragmentos** (el hueco más grande identificado en la auditoría - antes solo existía cambio de toma completa por región entera):
  - `selectTake` reescrito: antes silenciaba/activaba TODOS los clips de un `takeGroupId` a la vez (cambio de toma "todo o nada" por región completa). Ahora solo actúa sobre los clips del grupo que **se solapan en tiempo** con el clip recién elegido - los fragmentos de otras zonas del mismo grupo de tomas quedan intactos.
  - `splitClipAtPlayhead` reescrito: antes encontraba y dividía solo el PRIMER clip de la pista que cubriera el playhead (por orden de array, sin mirar si estaba silenciado o no - podía partir una toma inaudible en vez de la que se oye). Ahora divide TODAS las tomas apiladas bajo el playhead a la vez, igual que cortar una línea de comping en un DAW real corta todas las tomas superpuestas de una vez, no solo la de encima.
  - `removeClip`: la promoción de toma tras borrar la activa ahora también respeta el solapamiento temporal (antes promovía cualquier clip del grupo, pudiendo "resucitar" un fragmento de una zona distinta a la borrada).
  - `ClipView.tsx`: el selector de "Toma N/M" de un clip ahora solo lista tomas que se solapan con ESE clip específico, no todo el grupo entero - evita ofrecer como alternativa un fragmento de otra parte de la línea de tiempo que ya fue comped de forma independiente.
  - Flujo real resultante: grabar varias tomas superpuestas (ya existía) → dividir en el punto de comp deseado con "Split" (ahora corte-todas-las-tomas-a-la-vez) → elegir la toma activa por fragmento con el picker de "Toma N/M" de cada trozo. Es real comping fragmento a fragmento reutilizando la arquitectura de tomas existente, no un sistema nuevo en paralelo.
  - `src/types/project.ts`: comentario de `takeGroupId` actualizado para documentar el nuevo comportamiento.
  - Nuevo test en `projectStore.test.ts`: graba 2 tomas completas superpuestas (10s), corta ambas a la vez en t=5, comprueba que quedan 4 fragmentos con el mismo `takeGroupId`, y que elegir una toma distinta para la mitad derecha no afecta el estado de mute de la mitad izquierda (y viceversa) - la prueba concreta de que el comping es real por fragmento, no una ilusión de UI sobre el mismo mecanismo todo-o-nada.
  - `tsc`/`eslint`/`vitest` (330 tests, 1 nuevo) limpios.

## FASE 10G — Afinación (EN CURSO, por modo)

La familia de 10 modos del brief (punto 4) se implementa modo a modo sobre el `pitchCorrection` insert ya existente (`realtime-pitch-processor.js`), reutilizando su detección YIN + desplazador de línea de retardo, en vez de construir un motor nuevo por modo.

**Ya cubiertos antes de este pivote, por los presets y knobs existentes** (no es trabajo nuevo, solo mapeo a la lista del brief): 1) Corrección por escala (tonalidad/escala/retune/humanize base), 2) Corrección dura (`trapDuro`/`robot`, retune≈0), 3) Corrección natural (`natural`/`transparente`, retune alto).

- [x] **Modo 5 — Cambio de tono fijo**:
  - `PitchCorrectionParams` ganó `mode: "scale" | "fixed"` y `fixedSemitones`. En `"fixed"`, el efecto ignora tonalidad/escala/retune/humanize por completo - transposición constante, no corrección.
  - `realtime-pitch-processor.js`: nuevos `AudioParam`s `mode`/`fixedSemitones`. En modo fijo, `runAnalysisHop` sigue corriendo YIN (para el lector de nota detectada en vivo, honesto con lo que realmente está sonando) pero calcula `pitchRatio` directo como `2^(semitonos/12)` en vez de derivarlo de un snap a escala - reutiliza el mismo desplazador de línea de retardo con deriva continua que ya usa la corrección por escala, no un camino de código nuevo.
  - **Límite real encontrado y corregido antes de dejarlo mal expuesto en la UI**: el desplazador satura en `MIN_PITCH_RATIO=0.7`/`MAX_PITCH_RATIO=1.4` (límites ya existentes del propio archivo, verificados matemáticamente: `12·log2(0.7) ≈ -6.17` semitonos, `12·log2(1.4) ≈ +5.83`). Un primer borrador ofrecía el knob de ±12 semitonos, que habría saturado en silencio pasado ±6 sin decírselo al usuario. Corregido: knob y `AudioParam` acotados a ±6, con nota explícita en el panel de por qué el límite existe.
  - `PitchCorrectionPanel.tsx`: selector de modo (Corrección por escala / Tono fijo) arriba de todo; en modo fijo solo se muestran el knob de transposición y mezcla (tonalidad, escala, teclado, retune, humanize, rango de detección desaparecen porque no significan nada en este modo), con la misma advertencia honesta de "sin preservación de formantes" que ya tenía el modo por escala (ninguno de los dos la tiene).
  - Verificado en navegador (Chromium headless, mic falso): pista de audio → Afinación añadida como insert → alternar a "Tono fijo" muestra el knob de "Transposición", sin errores de consola. `tsc`/`eslint`/`vitest` (330 tests) limpios.
  - **Pendiente explícito, no fingido**: "con y sin corrección de formantes" del punto 5 del brief - solo está construido el "sin". La corrección de formantes no existe en ningún punto del motor todavía (ver tarea pendiente #80, "Formant preservation in PSOLA", que cubre tanto esto como el modo 6 completo).

- [x] **Modo 7 — Armonizador** y **Modo 8 — Doblaje** (resueltos juntos: ambos son renders offline nuevos, no inserts en vivo, y comparten la misma infraestructura de detección+PSOLA que ya usa el Estudio de afinación):
  - `noteUtils.ts` ganó `scaleStepUp(midi, key, scale, steps)`: mueve una nota N **grados de escala** (no semitonos fijos) - una tercera sobre C en Do mayor es Mi (+4 semitonos) pero una tercera sobre D es F (+3 semitonos), el ancho real cambia según el grado, que es justo lo que dice el punto 7 del brief ("terceras/quintas/octavas **dentro de la escala**", no un intervalo cromático fijo que se saldría de la tonalidad). 8 tests nuevos, incluyendo que una octava (7 grados en una escala de 7 notas) da siempre +12 semitonos exactos.
  - `harmonize.ts` (nuevo): `buildHarmonyCurve(frames, key, scale, scaleSteps)` - por cada frame detectado, ajusta a la escala más cercana y luego sube/baja N grados con `scaleStepUp`; `harmonizeChannel`/`harmonizeBuffer` alimentan esa curva al mismo `psolaShift` que ya usa la corrección de tono. No es un motor de pitch-shift nuevo, es la reutilización directa del que ya existía. 5 tests.
  - `doubler.ts` (nuevo): `buildDoubleCurve(frames, detuneCents, random)` - variación lenta y suave (mismo random-walk con decaimiento que ya usa `humanize` en `correctionCurve.ts`) centrada en la nota **detectada**, no en ninguna nota de escala - un doblaje debe seguir la misma melodía, solo no clavado exactamente en los mismos cents, como dos tomas humanas reales de la misma línea. Documentado explícitamente que la micro-variación de **tiempo** NO vive aquí (no hay time-warping dentro del render, sería una tarea bastante mayor) sino en dónde se coloca el clip resultante en la línea de tiempo - ver siguiente punto. 4 tests.
  - `PitchStudioPanel.tsx`: refactorizado su flujo de "renderizar y crear pista nueva" (antes solo para corrección de tono) a un helper compartido `renderChannelsToNewTrack`, reutilizado por los tres modos en vez de triplicar el código de encode/cache/persistir/colocar clip. Dos secciones nuevas: **Armonizador** (selector de intervalo 3ra/5ta/8va, botón "Generar armonía", crea una pista nueva a -4dB - un punto de partida de mezcla razonable para que no compita con la voz principal, no una afirmación de "balance correcto") y **Doblaje** (knob de variación en cents, botón "Generar doble", crea una pista nueva con `startTimeSec` desplazado unos milisegundos aleatorios (±30ms) - la variación de tiempo real y honesta, hecha en la colocación del clip, no simulada dentro del algoritmo de tono).
  - Ambas nuevas pistas se suman a la mezcla (no reemplazan la toma original, igual que la corrección de tono ya hacía) - así es como se usan de verdad armonías y dobles en producción vocal.
  - Verificado en navegador (Chromium headless, WAV de tono puro C4 sintetizado e importado vía "Importar audio"): abrir "Tono" en la biblioteca, generar armonía crea la pista "… (armonía 3ra)", generar doble crea "… (doble)", ambas con audio real decodificado y cacheado, sin errores de consola. `tsc`/`eslint`/`vitest` (347 tests, 17 nuevos) limpios.
  - **Pendiente**: el armonizador genera una sola voz de armonía por render (el usuario puede repetir la acción con otro intervalo para apilar varias) - no hay todavía un modo "coro" de N voces en un solo clic; el doblaje es de una sola copia (dos voces totales con el original), no un "quad-double" de cuatro capas típico de producción pop/rock pesado. Ambos son extensiones directas si se piden, no un límite de arquitectura.

- [x] **Modo 9 — Vocoder / robot** (efecto insert nuevo, no un preset sobre corrección de tono - un vocoder de verdad suena distinto a "voz afinada al instante", que es lo que "Corrección dura"/modo 2 ya cubre):
  - `VocoderEffect.ts` (nuevo): vocoder de canal clásico, análisis/síntesis, construido enteramente con nodos nativos de Web Audio (sin worklet). 12 bandas espaciadas logarítmicamente (150Hz-6kHz): cada banda de la voz (moduladora) pasa por un `BiquadFilterNode` pasabanda → rectificador de onda completa (`WaveShaperNode` con curva `abs(x)`) → pasabajos ~25Hz que suaviza eso en una envolvente real de "qué tan fuerte está esta banda ahora mismo". Una portadora fija (sierra/cuadrada) pasa por las mismas 12 bandas pasabanda, y cada banda de portadora se multiplica por su envolvente correspondiente usando la técnica estándar de Web Audio de conectar la salida de un nodo directamente al `AudioParam .gain` de otro (eso *es* la multiplicación de dos señales de audio en tiempo real - no existe un nodo "multiplicar" dedicado). Las 12 bandas moduladas se suman - esa suma es la voz de vocoder clásica.
  - Limitación real documentada explícitamente en el propio código: la portadora es una nota fija, no sigue el tono de quien canta (un vocoder "de verdad" más elaborado podría hacerlo); 12 bandas es una elección honesta para un insert de DAW vocal, no el estándar de 16-24 bandas de un vocoder de estudio dedicado.
  - `VocoderParams` (`carrierType: "sawtooth"|"square"`, `carrierFreqHz`, `mix`) añadido a `EffectInstance`/`EFFECT_LABELS`/`createEffectInstance`, cableado en `EffectChain.ts` igual que cualquier otro efecto nativo (aparece en "+ Nuevo efecto" en la pista, se guarda, se automatiza, se hace bypass - "es un insert más de la cadena", como pide el cierre del punto 4 del brief).
  - `VocoderPanel.tsx`: espectro real de la salida del propio vocoder (analizador en serie tras el efecto, mismo criterio de honestidad que el resto de paneles de esta fase) - un vocoder funcionando de verdad muestra un peine de picos en los centros de banda fijos, no una envolvente suave, porque eso es estructuralmente lo que produce un banco de portadoras filtradas por banda. Selector de forma de onda de portadora, knob de frecuencia de portadora, knob de mezcla.
  - Verificado en navegador (Chromium headless, mic falso): efecto añadido a una pista desde "+ Nuevo efecto" → "Vocoder", panel con knobs de Portadora/Mezcla visibles, alternar Sierra/Cuadrada sin errores de consola. `tsc`/`eslint`/`vitest` (352 tests) limpios.

- [x] **Modo 10 — Control por notas** (render offline, mismo patrón que armonía/doblaje):
  - `noteFollow.ts` (nuevo): `buildNoteFollowCurve(frames, notes)` - por cada frame de tono detectado, busca la nota de un `MidiClip` que esté sonando en ese instante exacto (`Note.startTime` es local al clip, así que la referencia se alinea desde su propio inicio con el comienzo de la toma) y corrige hacia esa nota exacta, ignorando tonalidad/escala por completo. Donde el patrón no tiene ninguna nota activa (un hueco, o pasado su última nota), el frame se deja **sin corregir** en vez de sostener la última nota o adivinar - un silencio editorial en la melodía de referencia significa "no corregir aquí", no "asume que debería seguir en la última nota escrita". 5 tests.
  - `PitchStudioPanel.tsx`: sección "Control por notas" con un `Picker` que lista todos los patrones MIDI del proyecto (de cualquier pista de instrumento con piano roll) y un botón "Aplicar corrección por notas" que renderiza una toma nueva (reemplaza, no se suma, igual que la corrección por escala) usando ese patrón como referencia. Si el proyecto no tiene ningún patrón MIDI todavía, el panel lo dice explícitamente en vez de mostrar un selector vacío.
  - `tsc`/`eslint`/`vitest` (352 tests, incluyendo los 5 de `noteFollow.test.ts`) limpios.

**FASE 10G — Afinación: los 10 modos del brief están completos** (1-3 ya cubiertos por presets/knobs previos a este pivote; 4 corrección gráfica pendiente de UI de edición manual - tarea #75; 5 tono fijo, 7 armonizador, 8 doblaje, 9 vocoder y 10 control por notas nuevos en esta sesión; 6 formantes/timbre sigue sin construir en ningún punto del motor - tarea #80, el único modo genuinamente no implementado de los diez).

## FASE 10E — Clip y hojas

Criterio del brief: "tocar el clip abre acciones y ajustes sin cambiar de pestaña - ninguna operación sobre algo visible exige navegar."

- [x] **Hoja contextual de clip** (`ClipContextSheet.tsx`, nuevo):
  - Antes, la única forma de editar ganancia/fundidos de un clip era arrastrar controles que solo aparecían con `group-hover` (el `opacity-0 group-hover:opacity-100` de los tirones de esquina de fundido) - eso literalmente no existe en una pantalla táctil, `:hover` nunca se dispara sin ratón. Y borrar un clip era doble clic, sin confirmación visual ni equivalente táctil fiable.
  - Ahora tocar el cuerpo del clip (sin arrastrar) abre una hoja con: nombre editable, selector de toma (si el fragmento tiene varias, reutilizando el mismo filtro por solapamiento temporal del comping real), ganancia/fundido de entrada/fundido de salida como mandos reales (no drag oculto), **Normalizar** (calcula el pico real del audio del clip en su rango `[sourceOffset, sourceOffset+duration]` con `computePeakDb` sobre el buffer real, no un valor inventado, y sube la ganancia hasta -0.5dB de pico), **Duplicar** y **Dividir aquí** (solo si el cabezal está dentro del clip), y **Eliminar clip**.
  - `ClipView.tsx`: distingue toque de arrastre por distancia real de movimiento del puntero (<4px = toque, abre la hoja; ≥4px = arrastre normal, sigue moviendo el clip como antes) - el arrastre para mover/recortar/fundir con las esquinas sigue funcionando igual para quien usa ratón, la hoja es la vía garantizada para todo lo demás.
  - `duplicateClip(trackId, clipId)` nuevo en el store: duplica un clip específico por id sin depender de dónde esté el cabezal (`duplicateClipAtPlayhead` ya existente sigue existiendo para su propio atajo de teclado, sin tocar).
  - **Bug real encontrado durante la verificación en navegador, no al escribir el código**: `LoopRegion.tsx` renderiza un div de fondo que cubre TODA la altura del carril y todo el rango de tiempo del loop, sin `pointer-events-none` - eso capturaba cualquier clic dentro del rango del loop antes de que llegara al clip debajo, incluso donde el div no tenía ningún manejador propio. Como el loop por defecto de un proyecto nuevo ya cubre los primeros compases, esto bloqueaba la interacción con el primer clip que casi cualquiera coloca. Corregido: el div de fondo pasa a `pointer-events-none`, y las tres tiras interactivas reales (barra de mover arriba, bordes de inicio/fin) ganan `pointer-events-auto` explícito para seguir funcionando. Esto no es un bug introducido por esta subfase - ya afectaba el arrastre normal de clips antes también, solo que nadie lo había verificado interactuando de verdad dentro del rango del loop.
  - Verificado en navegador (Chromium headless, WAV de tono puro importado): clic corto sobre el clip abre la hoja con Normalizar/Eliminar visibles; Normalizar sube la ganancia a +3.9dB (matemáticamente correcto: pico de una onda a amplitud 0.6 es -4.44dB, objetivo -0.5dB, diferencia 3.94dB); arrastre real (80px) sigue moviendo el clip sin abrir la hoja; sin errores de consola. `tsc`/`eslint`/`vitest` (352 tests) limpios.
  - **Corrección de un error propio de esta sesión**: el slider de "Ganancia de entrada" añadido a `TransportBar.tsx` en la subfase de grabación usaba `<input type="range">` nativo - `ParamSlider.tsx` documenta explícitamente que la FASE 9 prohíbe controles range nativos en cualquier contexto de audio de este proyecto (hit-area/estética consistente con `Knob`). Corregido a `Knob`, mismo patrón que cualquier otro control de ganancia en la app.

## FASE 10H — Mezcla y nueva pista

Referencia del brief: "Canales en vertical con fader horizontal. En un teléfono nadie mezcla con columnas de escritorio" + "el bloque de mezcla automática va arriba del todo, no escondido al final."

- [x] **Mezclador reconstruido para móvil**:
  - **Gap real encontrado**: el `MixerPanel` existente ya tenía fader, medidor, pan, M/S/armar/monitor y FX por pista - pero como tiras verticales en una fila que se desplaza horizontalmente (patrón de escritorio de estación de trabajo), exactamente lo que el brief dice explícitamente que no sirve en un teléfono. Y el Asistente de Mezcla IA (`MixAssistantPanel`, ya construido en una fase anterior) solo vivía dentro de la pestaña "Mezcla IA" de Biblioteca - para usarlo desde la pantalla de Mezcla real (la de los faders) había que cambiar de pestaña, justo lo que "no escondido al final" prohíbe.
  - `Fader.tsx` ganó `orientation: "vertical" | "horizontal"` y `length` (antes solo vertical) - mismo cálculo de arrastre por delta, proyectado sobre el eje X en vez de Y para el modo horizontal, no una segunda implementación paralela.
  - `MixerPanel.tsx`: en móvil (`md:hidden`) ahora es una lista vertical de filas de canal, una por pista, cada una con fader horizontal real (no vertical apretado en una tira estrecha) + medidor horizontal + pan + M/S/Rec/Monitor + FX, terminando en la fila de Master. El `MixAssistantPanel` completo se renderiza arriba de todo, antes de la lista de canales. En escritorio (`hidden md:flex`) se conserva exactamente el layout de tiras verticales anterior sin cambios - apropiado ahí, donde sí hay ratón y pantalla ancha, y donde Biblioteca (con su "Mezcla IA") ya está siempre visible al lado, no escondida.
  - Verificado en navegador (Chromium headless, viewport 390×844 tipo teléfono): pista "Pista 1" agregada → pestaña Mezcla muestra "ASISTENTE DE MEZCLA IA" con su botón "Analizar mezcla" arriba de todo, fila de canal con fader horizontal real y controles M/S/Rec/Monitor, fila de Master debajo, sin errores de consola. `tsc`/`eslint`/`vitest` (352 tests) limpios.

**Con esto, FASE 10 (10A-10I) queda cubierta salvo 10I (auditoría en dispositivo real - bloqueada en que el propio usuario la haga en su teléfono) y los huecos ya documentados explícitamente como pendientes dentro de cada subfase (corrección gráfica y formantes en Afinación, marcadores/pads/troceado del Sampler pausados por el pivote de prioridades).

## Pivote a DIRECCIÓN MAESTRA DEL PROYECTO

El usuario compartió el artifact de referencia visual de Cabina y un documento maestro de 47 secciones que redefine la prioridad del proyecto: DAW de grabación/edición vocal real primero (P0: grabación, reproducción, línea de tiempo, clips, pistas, deshacer, monitoreo), IA después. Auditoría honesta hecha directamente sobre el código (no de memoria): tokens/fuentes Cabina están de verdad cableados, pero la pantalla "Voz" del artifact no existe como vista unificada; el zoom de línea de tiempo no existe (`PIXELS_PER_SECOND` es una constante fija); no hay una fuente única de conversión tiempo↔píxel (duplicada en 8+ archivos); el cabezal no sigue el scroll durante la reproducción; grabar sin pista armada descarta la toma en silencio sin avisar. El plan acordado prioriza, de mayor a menor costo: 1) zoom de línea de tiempo + fuente única + auto-scroll, 2) pantalla "Voz" unificada, 3) endurecimiento de casos borde de grabación, 4) terminar la migración visual a tokens Cabina, 5) auditoría de gestos de clip, 6) indicador de monitoreo, 7) fixes menores.

Antes de arrancar ese plan, el usuario reportó dos bugs reales de uso diario que se investigaron y corrigieron primero:

- [x] **Bug: "solo se oye por un audífono al grabar"** - causa real encontrada en `AudioEngine.ts`: `startRecording()` abría su propio `getUserMedia()` independiente, completamente aparte del stream ya abierto por el monitoreo (`MonitorSession`). Y no es un caso raro: el modo de monitoreo por defecto es `"auto"`, que se activa precisamente *porque* `isRecording()` pasa a ser verdadero (`shouldMonitorTrack`) - así que grabar con una pista armada en modo automático siempre terminaba con dos streams de mic concurrentes abiertos sobre el mismo dispositivo físico, en cada grabación. Dos streams concurrentes del mismo micrófono es un disparador real y documentado de que navegadores/SO móviles cambien la sesión de audio a un modo de enrutamiento tipo llamada de voz, que por defecto en muchos dispositivos sale por un solo canal/auricular en vez de estéreo completo - encaja exactamente con el síntoma reportado.
  - Arreglo: `RecordingSession` ya no abre ni posee su propio stream/source/inputGain - reutiliza `ensureMonitorStream()` (la misma que ya usa el monitoreo) y solo añade sus propias tomas (`worklet`, `analyser`) sobre el `inputGain` compartido. `stopRecording()`/`discardRecording()` ahora solo desconectan sus propias tomas, nunca el stream compartido (que sigue vivo mientras alguna pista armada quiera monitorear). Efecto colateral positivo: antes la grabación ignoraba `monitorConstraints` del usuario y hardcodeaba su propia copia (siempre desactivadas) - ahora graba exactamente con lo que se está escuchando, una sola fuente de verdad en vez de dos que podían divergir.
  - Caso borde nuevo cubierto: cambiar de dispositivo de entrada o de constraints del mic *mientras se graba* volvía a abrir un `inputGain` nuevo vía `stopMonitorStream()`/`ensureMonitorStream()`, lo que habría dejado a la grabación en curso desconectada y capturando silencio el resto de la toma - `retapRecordingSession()` nuevo reconecta la sesión de grabación activa al `inputGain` recién creado.
  - Verificado en navegador (Chromium headless, mic falso, contador de llamadas reales a `getUserMedia`): armar pista + grabar 2s + detener = **1 sola llamada a `getUserMedia`** en todo el ciclo (antes habría sido 2), clip creado correctamente, sin errores de consola.

- [x] **Bug: "el autotune nunca se oye bien"** - causa real encontrada en `realtime-pitch-processor.js`: el ratio de desplazamiento de tono (`pitchRatio = targetHz / detectedHz`) se recalculaba cada ~11.6ms dividiendo directamente por el tono **crudo, sin suavizar** que devuelve YIN ese instante. YIN sobre una voz real tiene ruido normal (vibrato, sibilantes, errores de octava ocasionales), y como el desplazador de línea de retardo de este worklet aplica el ratio de forma continua (no PSOLA con marcas sincronizadas al período, que sí absorbe ese ruido - ver el propio comentario del archivo sobre esa diferencia), cualquier ruido de detección se traducía directamente en un tartamudeo audible de velocidad en cada bloque de análisis. El objetivo (`smoothedTargetMidi`) ya se suavizaba con `retuneSpeedMs`; el tono detectado que hacía de denominador, no.
  - Arreglo: nuevo `smoothedDetectedMidi`, suavizado exponencialmente con una constante de tiempo propia (40ms - rápida para seguir un cambio real de nota, lenta para rechazar el ruido de un solo bloque de análisis), usado tanto para decidir a qué nota de la escala ajustar como para calcular el ratio (ahora en dominio de semitonos: `2^((targetMidi - detectedMidi)/12)`, no división directa de Hz crudos).
  - Verificado en navegador con medición real, no solo oído: tono sintético de 225Hz con micro-variación (simulando vibrato/ruido de detección) a través del worklet real en un `AudioContext`, comparando antes/después del fix (mismo test, con y sin el cambio vía `git stash`) sobre las 53 lecturas de `pitchRatio` reportadas: desviación estándar del ratio bajó de **0.052 a 0.029** (-43%), el salto máximo entre bloques consecutivos bajó de **0.234 a 0.090** (-62%), la media se mantuvo igual de correcta (~0.99, objetivo real 0.978). Sin errores de consola. `tsc`/`eslint`/`vitest` (352 tests) limpios tras ambos fixes.
  - **Lo que este fix no resuelve, dicho explícitamente**: sigue sin ser PSOLA (ver el límite ya documentado en el propio archivo - latencia ~30-50ms, un crossfade ocasional al re-basar el retardo cada varios cientos de ms). El suavizado reduce el ruido de detección, no elimina la diferencia de calidad entre este monitor en vivo y el render offline de Estudio de Afinación, que sigue siendo la opción de mayor calidad una vez grabada la toma.

## Línea de tiempo: zoom real + fuente única tiempo↔píxel + auto-scroll del playhead

Primer punto del plan acordado tras el pivote - el gap estructural que el propio documento maestro señala en su sección 7: `PIXELS_PER_SECOND` era una constante fija (`Timeline/constants.ts`), importada directamente en 8 archivos distintos (`Timeline.tsx`, `Ruler.tsx`, `TrackLane.tsx`, `ClipView.tsx`, `MidiClipView.tsx`, `LoopRegion.tsx`, `AutomationEditor.tsx`), cada uno multiplicando por su cuenta - funcionaba solo porque el valor nunca cambiaba, no porque hubiera una fuente única de verdad real.

- [x] **Zoom real, con una sola fuente de verdad**:
  - `pixelsPerSecond`/`setPixelsPerSecond` nuevos en `projectStore.ts`, en la misma categoría ya establecida que `snapResolution` ("estado de UI de la línea de tiempo, deliberadamente fuera de deshacer/rehacer y de la persistencia" - el zoom no es una decisión del proyecto, es cómo lo estás mirando ahora mismo). El setter clampa a `[MIN_PIXELS_PER_SECOND=20, MAX_PIXELS_PER_SECOND=400]` (constante anterior de 80px/s conservada como `DEFAULT_PIXELS_PER_SECOND`, así que el zoom inicial no cambia).
  - Los 8 archivos que antes importaban la constante fija ahora leen `pixelsPerSecond` del store reactivamente - no se creó ningún helper nuevo de conversión tiempo↔píxel (`time * pixelsPerSecond` inline, igual que ya se hacía) porque la aritmética nunca fue el problema real; el problema era tener 8 copias de un número fijo en vez de un único valor compartido y reactivo. Verificado con medición real: 2 clics de "acercar" (factor 1.25 multiplicativo) dan un ancho de clip exactamente ×1.5625 el original en los 8 archivos a la vez (ruler, pistas, clips, loop, todos re-renderizan en sincronía).
  - `ZoomControl.tsx` (nuevo): par de botones −/+ con lectura de porcentaje, en la barra inferior de la línea de tiempo junto al selector de "Ajuste" - el control confiable y siempre disponible, no dependiente de gestos táctiles.
  - `usePinchZoom.ts` (nuevo hook): pellizco de dos dedos real sobre el contenedor de scroll de la línea de tiempo, escuchando `PointerEvent`s nativos filtrados por `pointerType==="touch"` (el mouse no dispara esto). El zoom queda anclado al punto medio entre los dos dedos (no "siempre desde el borde izquierdo", que haría saltar la vista en cada pellizco) - recalcula qué instante de tiempo cae bajo el punto medio antes de aplicar el nuevo zoom y ajusta `scrollLeft` para que ese mismo instante siga bajo los dedos después. El contenedor de scroll gana `touch-action: pan-x pan-y` explícito (excluye el pellizco nativo del navegador, que si no competiría con este) sin bloquear el scroll normal de un dedo en ningún eje.
  - Verificado en navegador con eventos táctiles sintéticos reales (no simulación de alto nivel): despachar `pointerdown`/`pointermove`/`pointerup` con `pointerType:"touch"` para dos punteros separándose de 40px a 320px de distancia produjo un ancho de clip ×5 (el tope exacto: `MAX_PIXELS_PER_SECOND=400 / DEFAULT=80 = 5`) - confirma tanto el seguimiento multi-puntero como el clampeo correcto en el límite.
  - **Auto-scroll del playhead durante reproducción**: nuevo `useEffect` en `Timeline.tsx` que revisa en cada tick del transporte (cada frame mientras reproduce, ya que `currentTime` cambia cada frame) si la posición en píxeles del playhead se acerca al borde derecho del viewport visible o cae antes del área de pistas (detrás del header sticky) - si es así, ajusta `scrollLeft` para recentrar el playhead hacia el tercio izquierdo del viewport, mismo criterio de "seguir" que Pro Tools/Ableton (no fija el playhead a un píxel exacto cada frame, lo que pelearía con un scroll manual del usuario apenas empieza a reproducir). Verificado en navegador con zoom alto + reproducción real durante 3.5s: `scrollLeft` pasó de 0 a 1243px sin intervención manual.
  - `tsc`/`eslint`/`vitest` (352 tests) limpios. Un error real de lint durante el desarrollo (`Cannot access refs during render` - el hook mutaba `ref.current` directamente en el cuerpo del componente en vez de un `useEffect`) fue corregido antes de dar esto por terminado.
